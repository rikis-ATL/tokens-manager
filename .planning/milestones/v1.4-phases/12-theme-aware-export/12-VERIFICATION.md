---
phase: 12-theme-aware-export
verified: 2026-03-20T00:00:00Z
status: human_needed
score: 10/10 must-haves verified
re_verification: false
human_verification:
  - test: "Open Config page for a collection with at least one theme. Confirm 'Export theme:' selector is visible above the Build Output panel, defaults to 'Collection default', and lists each theme name."
    expected: "Selector renders with 'Collection default' as default; all theme names appear as options."
    why_human: "Conditional render (themes.length > 0) requires live data in-browser; cannot assert over static file analysis."
  - test: "Open Config page for a collection with zero themes."
    expected: "No 'Export theme:' selector appears anywhere on the page."
    why_human: "Conditional guard (themes.length > 0) hides the selector; needs browser with real data to confirm absence."
  - test: "On a collection with themes, keep selector at 'Collection default', click Build Tokens. Inspect the CSS output tab."
    expected: "No '/* Generated by Tokens Manager, theme: ... */' comment at the top of the CSS output."
    why_human: "Comment injection path depends on themeLabel being undefined in request body; needs live build round-trip."
  - test: "On a collection with themes, select a named theme from the 'Export theme:' selector. Wait for auto-build. Inspect CSS, SCSS, JS, TS tabs."
    expected: "First line is '/* Generated by Tokens Manager, theme: [theme name] */'. Switch to JSON tab — no comment present."
    why_human: "Comment injection and format-guard logic requires real SD build output to inspect; not statically verifiable."
  - test: "Open Figma export dialog from any collection Config page. Locate the Export button."
    expected: "'Figma Variables API requires Enterprise plan.' text is visible above the Cancel/Export button row. Export button is NOT disabled."
    why_human: "Visual placement and button state require in-browser rendering to confirm."
---

# Phase 12: Theme-Aware Export Verification Report

**Phase Goal:** Export is targeted at a specific theme or the collection default, and Figma export represents each enabled theme as a variable mode
**Verified:** 2026-03-20
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Config page has a theme selector; user can choose "Collection default" or any named theme as the export target | VERIFIED (automated) + human check needed | `config/page.tsx` line 76: `{themes.length > 0 && (` guards the `<Select>` block; `selectedThemeId` state defaults to `'__default__'`; selector wired to `onValueChange={(v) => setSelectedThemeId(v)}` |
| 2 | SD build with theme selected uses theme's values for Enabled groups and collection-default values for Source groups | VERIFIED (automated) | `mergeThemeTokens` in `themeTokenMerge.ts` (line 39-50): `disabled` → skipped, `source` → pushes `masterGroup`, `enabled` → pushes `themeGroupMap.get(id) ?? masterGroup`. Called client-side in `config/page.tsx` line 51 before passing `mergedTokens` to `BuildTokensPanel`. |
| 3 | Figma Variables export produces a payload where each enabled theme becomes a distinct variable mode | VERIFIED (automated) | `export/figma/route.ts`: `buildMultiModePayload` constructs `variableModes` array with a `Default` entry plus one entry per theme (line 175-183); `variableModeValues` populated per theme via `mergeThemeTokens` call per theme (line 144). |

**Score:** 3/3 success criteria verified (automated checks pass; 5 items deferred to human for browser confirmation)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/themeTokenMerge.ts` | Pure merge helper — exports `mergeThemeTokens` | VERIFIED | 51 lines; exports `mergeThemeTokens`; null pass-through on line 26; group-state dispatch on lines 39-47; calls `tokenService.processImportedTokens` + `tokenService.generateStyleDictionaryOutput` |
| `src/types/token.types.ts` | `BuildTokensRequest` extended with `themeLabel?: string` | VERIFIED | Line 226: `themeLabel?: string;` with inline comment present |
| `src/app/api/build-tokens/route.ts` | Comment header injection for CSS/SCSS/LESS/JS/TS | VERIFIED | `COMMENT_FORMATS = new Set(['css', 'scss', 'less', 'js', 'ts'])` on line 5; injection block on lines 25-33; JSON excluded by format guard |
| `src/app/collections/[id]/config/page.tsx` | Theme fetch + theme selector state + theme-aware SD trigger | VERIFIED | Parallel fetch at line 39-44; `selectedThemeId` state at line 19; `mergedTokens` derivation at lines 50-52; selector JSX at lines 76-94; `BuildTokensPanel` receives `mergedTokens` + `themeLabel` at lines 95-100 |
| `src/components/dev/BuildTokensPanel.tsx` | Accepts `mergedTokens` and `themeLabel` props | VERIFIED | `themeLabel?: string` in `BuildTokensPanelProps` line 12; spread into POST body at line 51; in `useCallback` deps at line 72; in `useEffect` deps at line 85 |
| `src/components/figma/ExportToFigmaDialog.tsx` | Enterprise plan inline note below Export button | VERIFIED | Line 254-256: `<p className="text-xs text-gray-500">Figma Variables API requires Enterprise plan.</p>` wrapped in `flex flex-col` div above `DialogFooter`; Export button not disabled by note (line 267: `disabled={!selectedCollectionId || exporting}`) |
| `src/app/api/export/figma/route.ts` | Multi-mode Figma Variables payload | VERIFIED | 247 lines; imports `mergeThemeTokens` at line 2; `buildMultiModePayload` returns `{ variableModes, variables, variableModeValues }` at line 210; self-fetches themes from MongoDB via `getRepository` when `mongoCollectionId` provided |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/themeTokenMerge.ts` | `src/services/token.service.ts` | `tokenService.processImportedTokens` + `generateStyleDictionaryOutput` | WIRED | Line 29: `tokenService.processImportedTokens(masterTokens, namespace)`. Line 50: `tokenService.generateStyleDictionaryOutput(mergedGroups, namespace)`. Both methods exist in `token.service.ts` (confirmed by grep). |
| `src/app/api/build-tokens/route.ts` | `src/lib/themeTokenMerge.ts` | NOT imported here — merge is client-side; route only reads `themeLabel` for comment injection | WIRED | Route reads `body.themeLabel` at line 25. Merge is correctly performed client-side in `config/page.tsx`. Design intent is correct per plan. |
| `src/app/collections/[id]/config/page.tsx` | `src/lib/themeTokenMerge.ts` | `import mergeThemeTokens`; called client-side before passing tokens to `BuildTokensPanel` | WIRED | Import on line 7; call on line 51 inside derived-state block; result (`mergedTokens`) passed as `tokens` prop to `BuildTokensPanel`. |
| `src/components/dev/BuildTokensPanel.tsx` | `src/app/api/build-tokens` | `POST /api/build-tokens` with `{ tokens: mergedTokens, themeLabel }` in request body | WIRED | Line 51: `body: JSON.stringify({ tokens, namespace, collectionName, ...(themeLabel ? { themeLabel } : {}) })`. When `themeLabel` is undefined (Collection default), it is omitted from the body correctly. |
| `src/app/api/export/figma/route.ts` | `src/lib/themeTokenMerge.ts` | `import mergeThemeTokens`; called per theme inside `buildMultiModePayload` | WIRED | Import on line 2; called at line 144 inside the `for (const theme of themes)` loop. |
| `src/app/api/export/figma/route.ts` | Figma POST `/v1/files/:fileKey/variables/local` | `fetch` with `{ variableModes, variables, variableModeValues }` payload | WIRED | Lines 37-44: `fetch(apiUrl, { method: 'POST', ... body: JSON.stringify(figmaPayload) })`. `figmaPayload` = result of `buildMultiModePayload` which returns `{ variableModes, variables, variableModeValues }`. |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| EXPORT-01 | 12-02, 12-04 | User can select which theme (or collection default) to target for export on the Config page | SATISFIED | `config/page.tsx` renders `<Select>` with `__default__` + per-theme `<SelectItem>` options when `themes.length > 0`. `selectedThemeId` defaults to `'__default__'` on every load (not persisted). |
| EXPORT-02 | 12-01, 12-04 | Style Dictionary export uses the selected theme's token values for the active export | SATISFIED | `mergeThemeTokens` returns merged token JSON respecting group states (disabled/source/enabled). `BuildTokensPanel` sends `mergedTokens` + optional `themeLabel` to `/api/build-tokens`. Route injects comment header into CSS/SCSS/LESS/JS/TS. |
| EXPORT-03 | 12-03, 12-04 | Figma Variables export generates one mode per enabled theme in the collection | SATISFIED | `export/figma/route.ts` self-fetches themes from MongoDB, constructs `variableModes` array with Default + one entry per theme. `variableModeValues` built using `mergeThemeTokens` per-theme. Graceful fallback to Default-only when no themes. |

No orphaned requirements: all three EXPORT-* requirements claimed in plan frontmatter and verified.

---

## Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| `src/app/api/export/figma/route.ts` | `doc as unknown as { namespace?: string }` (line 26) | Info | Type cast to access `namespace` from MongoDB doc; not ideal but functional. A typed repository interface would be cleaner. Not blocking. |
| `src/components/figma/ExportToFigmaDialog.tsx` | `placeholder=` attributes (lines 192, 220) | Info | Legitimate HTML form placeholder attributes, not stub indicators. |

No blockers or warnings found. No `TODO`/`FIXME`/`PLACEHOLDER` stubs anywhere in phase-12-modified files.

---

## TypeScript Compilation

`npx tsc --noEmit` — zero errors. All phase 12 files compile cleanly.

---

## Git Commits

All commits documented in SUMMARYs are present in git history:

| Commit | Plan | Description |
|--------|------|-------------|
| `05264bd` | 12-01 | feat: add themeTokenMerge pure helper and extend BuildTokensRequest |
| `2a283b4` | 12-01 | feat: inject theme comment header in build-tokens route for code formats |
| `58c555a` | 12-02 | feat: upgrade config page with theme selector and theme-aware SD build |
| `ded54b2` | 12-02 | feat: add Figma Enterprise plan note to ExportToFigmaDialog |
| `ad9ca53` | 12-03 | feat: rewrite Figma export route for multi-mode payload |
| `c7c9456` | 12-04 | docs: checkpoint human-verify Phase 12 theme-aware export |

---

## Human Verification Required

The following scenarios require a browser walkthrough with a running dev server (`npm run dev`).

### 1. Theme Selector Visible With Themes

**Test:** Open `/collections/[id]/config` for a collection that has at least one theme.
**Expected:** "Export theme:" label + dropdown appear above the Build Output panel. Dropdown shows "Collection default" as selected by default, with each theme name listed as an option.
**Why human:** Conditional render depends on `themes.length > 0` populated from a real API call — cannot assert in static analysis.

### 2. Theme Selector Hidden Without Themes

**Test:** Open `/collections/[id]/config` for a collection with zero themes.
**Expected:** No "Export theme:" selector appears anywhere on the page.
**Why human:** Same conditional render guard requires live data to confirm the absence branch.

### 3. Collection Default Build Has No Comment Header

**Test:** Keep selector at "Collection default". Click Build Tokens. View CSS tab.
**Expected:** CSS output does NOT start with `/* Generated by Tokens Manager, theme: ... */`.
**Why human:** No-comment path requires live SD build round-trip to inspect actual output content.

### 4. Theme Build Injects Comment Header; JSON Is Clean

**Test:** Select a named theme. Wait for auto-build (or click Build Tokens). Inspect CSS, SCSS, JS, TS tabs, then JSON tab.
**Expected:** CSS/SCSS/JS/TS first line is `/* Generated by Tokens Manager, theme: [theme name] */`. JSON tab has no such comment.
**Why human:** Comment injection logic requires live SD build output to inspect actual content; format-guard must be confirmed across multiple tabs.

### 5. Figma Export Dialog Shows Enterprise Plan Note

**Test:** Open any collection Config page. Click the Figma export button to open the dialog.
**Expected:** "Figma Variables API requires Enterprise plan." text is visible near the Export button. The Export button is still enabled (not greyed out by the note).
**Why human:** Visual placement and button state require in-browser rendering to confirm.

---

## Gaps Summary

No automated gaps found. All artifacts exist, are substantive (not stubs), and are correctly wired. TypeScript compiles cleanly. Zero anti-pattern blockers.

Five items are deferred to human verification because they require a live browser session with real database data (theme selector conditional render, SD build output content inspection, Figma dialog rendering). These are behavioral/visual verifications that cannot be automated through static analysis.

The automated portion of this verification is fully passing.

---

_Verified: 2026-03-20_
_Verifier: Claude (gsd-verifier)_
