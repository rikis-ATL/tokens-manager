---
phase: 06-selection-breadcrumbs-content-scoping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TokenGroupTree.tsx
  - src/app/collections/[id]/tokens/page.tsx
autonomous: true
requirements:
  - TREE-03

must_haves:
  truths:
    - "Clicking any tree node in the sidebar highlights it with a gray background"
    - "Unselected nodes show a subtle hover background when the pointer enters"
    - "The selected node ID is tracked in tokens page state and updates on each click"
  artifacts:
    - path: "src/components/TokenGroupTree.tsx"
      provides: "Click handler on each row invoking onGroupSelect; conditional bg-gray-200 on selected node, hover:bg-gray-100 on unselected"
      contains: "onGroupSelect"
    - path: "src/app/collections/[id]/tokens/page.tsx"
      provides: "onGroupSelect handler passed to TokenGroupTree that calls setSelectedGroupId"
  key_links:
    - from: "src/components/TokenGroupTree.tsx"
      to: "src/app/collections/[id]/tokens/page.tsx"
      via: "onGroupSelect prop callback"
      pattern: "onGroupSelect.*groupId"
---

<objective>
Wire tree node selection so clicking any row in the TokenGroupTree sidebar highlights it and updates the page's selectedGroupId state.

Purpose: This is the interaction foundation — selection state drives both breadcrumbs (Plan 02) and content scoping (Plan 03). Getting this right first means the rest of the phase builds on a solid click-to-select primitive.
Output: TokenGroupTree with clickable rows, selection highlight, hover states; tokens page wiring for onGroupSelect.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.2-phases/06-selection-breadcrumbs-content-scoping/06-CONTEXT.md
@.planning/phases/05-tree-data-model/05-02-SUMMARY.md
@src/components/TokenGroupTree.tsx
@src/app/collections/[id]/tokens/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add click handler and selection highlight to TokenGroupTree</name>
  <files>src/components/TokenGroupTree.tsx</files>
  <action>
    In `src/components/TokenGroupTree.tsx`, update the node `<div>` inside the `nodes.map(...)` render to:

    1. Add `onClick={() => onGroupSelect?.(group.id)}` to each node's outer `<div>`.
    2. Add `cursor-pointer` to all node rows always (regardless of selection state).
    3. Replace the static className string with a conditional:
       - Selected node (`group.id === selectedGroupId`): apply `bg-gray-200 text-gray-900`
       - Unselected node: apply `hover:bg-gray-100 text-gray-700`
    4. Keep the existing `font-semibold` / `font-normal` logic for parent vs leaf nodes.
    5. Keep the `style={{ paddingLeft: depth * 16 + 8 }}` and `py-1 pr-3 text-sm` classes unchanged.

    The resulting className expression should look roughly like:
    ```tsx
    className={`py-1 pr-3 text-sm cursor-pointer ${
      group.id === selectedGroupId
        ? 'bg-gray-200 text-gray-900'
        : 'hover:bg-gray-100 text-gray-700'
    } ${
      group.children && group.children.length > 0 ? 'font-semibold' : 'font-normal'
    }`}
    ```

    Do NOT add a left border — background-only highlight per user decision.
    Do NOT change the FlatNode interface, flattenTree function, or any other part of the component.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visually confirm node rows have cursor-pointer cursor style</manual>
  </verify>
  <done>TypeScript compiles with zero errors. TokenGroupTree onClick handler added; selected node applies bg-gray-200 text-gray-900; unselected nodes apply hover:bg-gray-100 text-gray-700.</done>
</task>

<task type="auto">
  <name>Task 2: Wire onGroupSelect handler from tokens page to TokenGroupTree</name>
  <files>src/app/collections/[id]/tokens/page.tsx</files>
  <action>
    In `src/app/collections/[id]/tokens/page.tsx`, update the `<TokenGroupTree>` JSX to pass the `onGroupSelect` prop:

    ```tsx
    <TokenGroupTree
      groups={masterGroups}
      namespace={globalNamespace}
      selectedGroupId={selectedGroupId}
      onGroupSelect={setSelectedGroupId}
    />
    ```

    `setSelectedGroupId` is already the state setter for `selectedGroupId` (React.Dispatch<SetStateAction<string>>). The `onGroupSelect` prop type in TokenGroupTree is `(groupId: string) => void` — this matches `setSelectedGroupId` (React state setters accept the value type).

    Also verify `handleGroupsChange` already auto-selects the first group on initial tree load (lines 135-144 of current page.tsx). If `selectedGroupId` is already empty string on first load and `handleGroupsChange` sets it to `groups[0]?.id ?? ''`, this is already correct — no changes needed there.

    No other changes to the tokens page in this plan.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>None — functional verification is in the checkpoint of Plan 03</manual>
  </verify>
  <done>TypeScript compiles with zero errors. onGroupSelect={setSelectedGroupId} prop wired. Clicking a tree node in the browser will update selectedGroupId state (verifiable in Plan 03 checkpoint).</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — must pass with zero errors.
Both TokenGroupTree.tsx and tokens page.tsx modified correctly.
No regressions to FlatNode interface, flattenTree, or namespace/empty-state rendering.
</verification>

<success_criteria>
Tree nodes are clickable. Selected node shows bg-gray-200 highlight. Unselected nodes show hover:bg-gray-100. onGroupSelect wired from tokens page. TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-selection-breadcrumbs-content-scoping/06-01-SUMMARY.md`
</output>
