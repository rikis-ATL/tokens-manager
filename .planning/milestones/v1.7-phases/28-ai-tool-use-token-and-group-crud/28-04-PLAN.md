---
phase: 28-ai-tool-use-token-and-group-crud
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified: []
autonomous: false
requirements: [AI-05, AI-06, AI-07, AI-08, AI-09, AI-10, AI-15]

must_haves:
  truths:
    - "User can create tokens via natural language in AI chat"
    - "User can delete tokens via AI chat with confirmation step"
    - "User can rename groups via AI chat"
    - "All AI tool calls go through existing API endpoints"
    - "Failed tool calls show readable errors in chat"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete AI tool use flow -- confirming the AI can create, edit, delete tokens and groups via chat.

Purpose: Validate all 5 Phase 28 success criteria from the roadmap are met end-to-end.
Output: Verified working AI tool use system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-ai-tool-use-token-and-group-crud/28-CONTEXT.md
@.planning/phases/28-ai-tool-use-token-and-group-crud/28-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify AI tool use end-to-end</name>
  <files></files>
  <action>
Human verification checkpoint. Start the dev server (`yarn dev`) and navigate to a collection's Tokens page. Run through 6 test scenarios to verify AI tool use works end-to-end. No code changes needed.
  </action>
  <verify>Human confirms all 6 test scenarios pass</verify>
  <done>All Phase 28 success criteria verified by human</done>
  <what-built>
Complete AI tool use system: the AI chat panel on the Tokens page can now create, edit, and delete tokens and groups in the active collection by calling structured tool functions that route through the app's own API endpoints.
  </what-built>
  <how-to-verify>
Start the dev server (`yarn dev`) and navigate to a collection's Tokens page.

**Test 1 -- Token creation (AI-05):**
1. Open the AI chat panel
2. Type: "Create a token called color.brand.primary with value #0056D2"
3. Expected: AI describes what it will do, calls the create_token tool, confirms the token was created
4. Verify: The token appears in the token table (may need page refresh)

**Test 2 -- Token editing (AI-06):**
1. Type: "Change color.brand.primary to #FF6600"
2. Expected: AI calls update_token tool, confirms the change
3. Verify: Token value updated in the table

**Test 3 -- Token deletion with confirmation (AI-07, D-11):**
1. Type: "Delete the token color.brand.primary"
2. Expected: AI lists the token to be deleted and asks "Shall I proceed?"
3. Type: "Yes, proceed"
4. Expected: AI calls delete_token tool, confirms deletion
5. Verify: Token removed from the table

**Test 4 -- Group rename (AI-09):**
1. If you have a group (e.g., "colors.brand"), type: "Rename the group colors.brand to colors.main"
2. Expected: AI calls rename_group tool, confirms the rename
3. Verify: Group path updated in the tree sidebar

**Test 5 -- Error handling (D-12):**
1. Type: "Delete the token nonexistent.path.here"
2. Expected: AI attempts the tool call, receives an error, and explains the issue in plain language

**Test 6 -- API endpoint routing (AI-15):**
1. Check browser Network tab during any tool use interaction
2. Expected: You should see fetch requests to `/api/collections/[id]/tokens` or `/api/collections/[id]/groups` -- the AI is NOT writing directly to MongoDB
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
All 5 roadmap success criteria for Phase 28 verified by human:
1. Token creation via natural language
2. Token deletion with confirmation
3. Group rename
4. Tool calls via API endpoints (not direct DB)
5. Failed tool calls as readable errors
</verification>

<success_criteria>
Human approves all 6 test scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/28-ai-tool-use-token-and-group-crud/28-04-SUMMARY.md`
</output>
