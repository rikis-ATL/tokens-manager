---
phase: 14-dark-mode-support
plan: 05
type: execute
wave: 3
depends_on:
  - 14-02
  - 14-03
  - 14-04
files_modified: []
autonomous: false
requirements:
  - DARK-06

must_haves:
  truths:
    - "All Phase 14 features verified working in browser: colorMode badges, create dialog, settings toggle, CSS dark output, JS/TS dark namespace output"
    - "TypeScript build passes with zero errors"
    - "Legacy themes (no colorMode in DB) render as 'Light' without crashes"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 14 dark mode support feature set.

Purpose: Phase 14 touches the type system, three API routes, four UI components/pages, the SD service, and the Figma export route. A single end-to-end checkpoint confirms everything works together before the phase is marked complete.
Output: Verified, working dark mode support implementation across all surfaces.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-dark-mode-support/14-01-SUMMARY.md
@.planning/phases/14-dark-mode-support/14-02-SUMMARY.md
@.planning/phases/14-dark-mode-support/14-03-SUMMARY.md
@.planning/phases/14-dark-mode-support/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification build check</name>
  <files></files>
  <action>
Run the full TypeScript and Next.js build check before handing off to human verification:

```bash
cd /Users/user/Dev/atui-tokens-manager
npx tsc --noEmit
yarn build
```

If either command fails, fix any TypeScript or build errors before proceeding to the checkpoint.

Common issues to check:
- `colorMode` accessed without `?? 'light'` on any code path that reads from MongoDB (ITheme from DB may lack the field)
- `onAdd` callback in ThemeList usage sites — signature changed from `(name: string)` to `(name: string, colorMode: ColorMode)`. Any old call sites need updating.
- Unused imports added to files (TypeScript strict mode warns on these)
- `buildCombinedOutput` — verify it's exported or not (it doesn't need to be exported, only called internally)
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -60 && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript: zero errors. Next.js build: successful.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: Phase 14 complete dark mode support — all scenarios</name>
  <action>Start dev server (`yarn dev`) and execute all 7 verification scenarios listed in how-to-verify below.</action>
  <what-built>
    Complete Phase 14 dark mode support:
    - ColorMode type on ITheme; POST/PUT theme routes accept colorMode
    - Create Theme dialog with light/dark selector (replaced inline input)
    - ColorModeBadge (sun=amber=light, moon=slate=dark) on theme items in ThemeList
    - "Switch to Light/Dark" in "..." dropdown → PATCHes colorMode to API
    - Theme selectors on tokens page and config page show colorMode badges
    - CSS/SCSS/LESS combined export: :root {} for light + [data-color-mode="dark"] {} for dark
    - JS/TS combined export: light exports + dark exports with "${namespace}Dark" prefix
    - Figma export: light+dark theme pairs exported as "Light" / "Dark" modes
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Start dev server: `yarn dev`
    2. Open a collection that has tokens. Navigate to /collections/[id]/themes.

    **Scenario 1 — Create Theme dialog:**
    3. Click "+" — confirm a DIALOG opens (not inline text input)
    4. Confirm dialog has a name input and Light/Dark toggle buttons
    5. Select "Dark" and enter name "Dark Theme" — click Create
    6. Confirm "Dark Theme" appears in the list with a moon icon (slate badge)

    **Scenario 2 — Color mode badge:**
    7. Confirm all existing themes (if any) show a sun/amber badge (defaulting to light for legacy themes)
    8. Create a "Light Theme" — confirm it shows a sun/amber badge

    **Scenario 3 — Color mode toggle:**
    9. Hover "Light Theme", click "..." — confirm menu has "Switch to Dark"
    10. Click "Switch to Dark" — confirm badge changes from sun to moon without page reload
    11. Click "Switch to Light" in "..." — confirm badge changes back to sun

    **Scenario 4 — Tokens page selector badges:**
    12. Navigate to /collections/[id]/tokens
    13. Open the theme selector dropdown — confirm each theme shows its mode badge

    **Scenario 5 — Config page combined CSS export:**
    14. Navigate to /collections/[id]/config
    15. Ensure "Collection default" is selected in "Export theme:" dropdown
    16. Click the build/preview button — inspect CSS output
    17. Confirm CSS output contains BOTH `:root {` section AND `[data-color-mode="dark"] {` section
    18. (If no dark theme exists in collection: CSS output should have only `:root {}` — unchanged)

    **Scenario 6 — Config page single-theme export:**
    19. In Export theme selector, pick the "Light Theme"
    20. Trigger build — CSS output should have ONLY `:root {}` (no dark block)

    **Scenario 7 — Legacy theme backward compatibility:**
    21. If any existing themes pre-date Phase 14 (no colorMode in DB), they should render as "Light" with sun badge — no crashes, no blank badges
  </how-to-verify>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>All 7 scenarios in how-to-verify confirmed working by user</manual>
  </verify>
  <done>All 7 human verification scenarios passed and "approved" signal received</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
TypeScript build: zero errors.
Next.js build: successful.
All 7 scenarios in human verification checkpoint approved.
</verification>

<success_criteria>
- `npx tsc --noEmit` passes
- `yarn build` succeeds
- All 7 human verification scenarios pass:
  1. Create Theme dialog (not inline input)
  2. Moon badge for dark themes, sun badge for light
  3. colorMode toggle via "..." dropdown
  4. Tokens page selector shows badges
  5. Combined CSS export (`:root` + `[data-color-mode="dark"]`) when dark theme in collection
  6. Single-theme CSS export when specific light theme selected
  7. Legacy themes show Light badge without crashes
- Phase 14 complete
</success_criteria>

<output>
After completion, create `.planning/phases/14-dark-mode-support/14-05-SUMMARY.md`
</output>
