---
phase: 999.1-css-html-pattern-token-types
plan: 02
type: execute
wave: 2
depends_on:
  - "999.1-01-PLAN.md"
files_modified:
  - src/types/graph-nodes.types.ts
  - src/lib/graphEvaluator.ts
  - src/components/graph/GroupStructureGraph.tsx
  - src/components/graph/nodes/PatternCssNode.tsx
  - src/components/graph/nodes/PatternHtmlNode.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "ComposableNodeConfig includes patternCss and patternHtml kinds"
    - "Graph palette lists two new nodes; React Flow registers composablePatternCss and composablePatternHtml"
    - "Pattern node outputs wire to Token Output values port as object { name, body } or { name, body, css? }"
  artifacts:
    - path: "src/lib/graphEvaluator.ts"
      provides: "evaluateNode cases for patternCss and patternHtml"
      contains: "patternCss|patternHtml"
---

<objective>
Add **two** composable graph node kinds (`patternCss`, `patternHtml`) per D-05: distinct from existing **`cssString`** (template/eval node). Nodes edit **name/body/(css)** and output **`value`** for **Token Output** targeting `cssClass` / `htmlTemplate` / `htmlCssComponent`. Persist in `graphState` like other composables.

Purpose: Graph parity with backlog + CONTEXT; users can generate pattern tokens from the canvas.
Output: New configs, UI components, evaluator, GroupStructureGraph registration, `buildTokensFromNode` supports object `value` in JSON pairs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.1-css-html-pattern-token-types/999.1-CONTEXT.md
@.planning/phases/999.1-css-html-pattern-token-types/999.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: graph-nodes.types.ts</name>
  <files>src/types/graph-nodes.types.ts</files>
  <action>
Add interfaces:
```ts
export interface PatternCssConfig {
  kind: 'patternCss';
  name: string;
  body: string;
}
export interface PatternHtmlConfig {
  kind: 'patternHtml';
  name: string;
  body: string;
  css: string; // optional in UX — empty string when unused
}
```
Extend `ComposableNodeConfig` union. Ensure `ComposableNodeConfig['kind']` exhaustiveness is updated everywhere TypeScript requires (switch defaults).
  </action>
</task>

<task type="auto">
  <name>Task 2: Evaluator</name>
  <files>src/lib/graphEvaluator.ts</files>
  <action>
- `evalPatternCss`: output `{ value: { name: trimmed name, body: trimmed body } }` (no `css` key).
- `evalPatternHtml`: output `{ value: { name, body, ...(css trimmed ? { css } : {}) } }` — include `css` only when non-empty, or always include optional field per TS type (match Plan 01 `PatternTokenValue`).
- Add cases in `evaluateNode`.
  </action>
</task>

<task type="auto">
  <name>Task 3: React node components</name>
  <files>src/components/graph/nodes/PatternCssNode.tsx, src/components/graph/nodes/PatternHtmlNode.tsx</files>
  <action>
Follow `CssStringNode` / `ConstantNode` patterns: `NodeWrapper`, handles for optional future wiring, textareas for `body` (and `css` on HTML). Use distinct border colors (e.g. slate vs emerald) to distinguish from **CSS string** node.
- **Output handle** `value` → connects to Token Output `values` input (verify handle ids match `TokenOutputNode` expectations — typically `values`).
  </action>
</task>

<task type="auto">
  <name>Task 4: GroupStructureGraph wiring</name>
  <files>src/components/graph/GroupStructureGraph.tsx</files>
  <action>
- Add `COMPOSABLE_BADGE`, `COMPOSABLE_NODES` entries (labels: e.g. “CSS pattern”, “HTML pattern”).
- Extend `KIND_TO_NODE_TYPE`, `NODE_WIDTHS`, `NODE_TYPES`, `defaultComposableConfig`.
- Import new node components.
- Update `buildTokensFromNode` JSON parse types: `value` may be `string | object` — pass through to `GeneratedToken.value`.
  </action>
</task>

<task type="auto">
  <name>Task 5: Token Output + handles</name>
  <files>src/components/graph/nodes/TokenOutputNode.tsx</files>
  <action>
Ensure `TOKEN_TYPE_OPTIONS` includes the three new types. Verify wiring from pattern node `value` output to tokenOutput `values` input produces **one** token with **object** value and correct `tokenType` selection (`cssClass`, `htmlTemplate`, or `htmlCssComponent`).
  </action>
</task>

<task type="human">
  <name>Human verify</name>
  <action>
In a group graph: add Pattern CSS → Token Output (`cssClass`); add Pattern HTML → Token Output (`htmlCssComponent`); generate tokens; confirm rows appear in table with object values.
  </action>
</task>

</tasks>
