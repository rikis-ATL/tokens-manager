---
phase: 999.1-css-html-pattern-token-types
plan: 03
type: execute
wave: 3
depends_on:
  - "999.1-01-PLAN.md"
  - "999.1-02-PLAN.md"
files_modified:
  - src/services/style-dictionary.service.ts
  - src/components/tokens/TokenGeneratorForm.tsx
  - src/app/api/build-tokens/route.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Pattern token types never appear in Style Dictionary build output"
    - "UI shows a visible non-exported indicator for pattern rows"
  artifacts:
    - path: "src/services/style-dictionary.service.ts"
      provides: "stripPatternTokensFromTree or equivalent before detectBrands"
      contains: "cssClass|htmlTemplate|htmlCssComponent|PATTERN"
---

<objective>
Implement **D-09**: omit pattern tokens from **all** `buildTokens` / Style Dictionary outputs (ZIP, live CSS, embed). Add **badge** (or inline status) in the token table for pattern types. Add **Jest** coverage for the strip helper. Optionally align AI/MCP `create_token` docs to mention pattern types are not SD-exported.

Purpose: Prevent broken SD output and set user expectations.
Output: Central filter + UI badge + tests; no preview iframe (still deferred per CONTEXT).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.1-css-html-pattern-token-types/999.1-CONTEXT.md
@src/services/style-dictionary.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip helper + buildTokens</name>
  <files>src/services/style-dictionary.service.ts</files>
  <action>
1. Import `PATTERN_TOKEN_TYPES` or `isPatternTokenType` from `token.types`.
2. Implement `stripPatternTokensForStyleDictionary(tokens: Record<string, unknown>): Record<string, unknown>` — recursively walk nested group objects; at **leaf** token objects, if `type` or `$type` matches a pattern type, **omit** that key from parent (or replace with empty object removal). Handle both `type`/`value` and `$type`/`$value` shapes consistent with `normalizeTokens`.
3. Call strip at the **start** of `buildTokens` on `tokens` and on `darkTokens` when present.
4. Add **unit tests** in existing test layout (e.g. `src/services/__tests__/style-dictionary.service.test.ts` or colocated) with a minimal nested tree containing pattern + color tokens; assert stripped tree has no pattern leaves.
  </action>
</task>

<task type="auto">
  <name>Task 2: Non-exported badge</name>
  <files>src/components/tokens/TokenGeneratorForm.tsx</files>
  <action>
Near type chip or value column, render a small **Badge** (e.g. “Pattern” or “Not exported”) when `isPatternTokenType(token.type)`. Use existing shadcn `Badge` + muted variant for consistency.
  </action>
</task>

<task type="auto">
  <name>Task 3: API parity</name>
  <files>src/app/api/build-tokens/route.ts, src/app/embed/.../tokens.js/route.ts, src/app/api/collections/[id]/tokens/live/route.ts</files>
  <action>
Confirm all code paths that call `buildTokens` pass collection token JSON — **no extra changes** if strip is inside `buildTokens`. If any route pre-flattens and bypasses `buildTokens`, apply the same helper there.
  </action>
</task>

<task type="auto">
  <name>Task 4: Optional — AI/MCP strings</name>
  <files>src/services/ai/tools.ts, src/mcp/tools/tokens.ts</files>
  <action>
If `create_token` / `update_token` expose `type` enum in descriptions, append one line: pattern types are **app-only** and excluded from SD export.
  </action>
</task>

<task type="human">
  <name>Human verify</name>
  <action>
Run export ZIP / Config preview with mixed tokens — confirm CSS/JSON outputs contain **no** pattern token vars. Confirm badge visible on pattern rows.
  </action>
</task>

</tasks>
