# Phase 999.1: CSS/HTML pattern token types — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.  
> Decisions are captured in `999.1-CONTEXT.md`.

**Date:** 2026-04-18  
**Phase:** 999.1 — css-html-pattern-token-types  
**Areas discussed:** storage shape, graph coverage, reference syntax, sandbox, export

---

## Context file already existed

**User choice:** Update existing context through discussion (not skip, not view-only).

---

## Value storage shape

| Option | Description | Selected |
|--------|-------------|----------|
| A | cssClass + htmlTemplate: string `$value`; htmlCssComponent: `{ html, css }` | |
| B | All three use a wrapper object for consistency | |
| C | Claude discretion | |
| **User response** | Two cases; object with **name** + **body** (or **value**); CSS and HTML **body** are strings; keep consistent with other tokens | ✓ |

**User's choice:** Consolidate to **two structural cases** with `{ name, body }` (optional `css` for HTML+CSS component on the HTML side).

**Notes:** Align field names with existing token `value` handling in TS and Mongo.

---

## Graph coverage

| Option | Description | Selected |
|--------|-------------|----------|
| A | New graph node only for CSS class; others table-only | |
| B | Dedicated node kind for each of three types | |
| C | CSS + HTML/CSS component nodes; HTML template table-only | |
| **User response** | Option **B** in spirit but **only two** node types: **CSS** and **HTML**; HTML can use CSS tokens | ✓ |

**User's choice:** Two graph kinds — **CSS** and **HTML** (not three). HTML encompasses template + component-style content.

---

## Reference syntax (HTML)

| Option | Description | Selected |
|--------|-------------|----------|
| A | Mustache `{{path}}` | Partial |
| B | `var()` / token paths | Partial |
| C | Freeform only | Partial |
| **User response** | **Both A and B if possible**; **freeform** HTML with class or var is the best **starting point** | ✓ |

**User's choice:** Prefer freeform HTML with classes/vars for v1; add mustache + var resolution where feasible later.

---

## Preview sandbox

| Option | Description | Selected |
|--------|-------------|----------|
| A | srcDoc iframe, no scripts | |
| B | srcDoc + CSP meta | |
| C | Defer preview | Partial |
| **User response** | OK to **defer**; prefer **CodePen** or similar **basic wrapper in app** | ✓ |

**User's choice:** Defer first slice; future embed should feel like CodePen-style wrapper.

---

## Style Dictionary / export

| Option | Description | Selected |
|--------|-------------|----------|
| A | Omit from SD outputs | ✓ |
| B | Sidecar files in ZIP | |
| C | Comments in CSS | |
| **User response** | **Omit** and **highlight** tokens as **non-exported** via **badge or status** | ✓ |

**User's choice:** Omit from exports; visible non-exported indicator in UI.

---

## Claude's Discretion

- CodePen vs alternatives; exact embed API; security review when preview ships.
- Graph edge semantics for HTML → CSS references.
- Badge copy and placement.

## Deferred Ideas

- Strict iframe sandbox (replaced by CodePen-like direction).
- Sidecar pattern files in ZIP.
- Visual/block editors from original problem statement.
