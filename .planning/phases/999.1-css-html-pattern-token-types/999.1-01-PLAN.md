---
phase: 999.1-css-html-pattern-token-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/token.types.ts
  - src/components/tokens/TokenGeneratorForm.tsx
  - src/components/tokens/TokenTable.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Three new TokenType union members exist: cssClass, htmlTemplate, htmlCssComponent"
    - "Persisted value shape is { name: string, body: string } with optional css?: string for htmlCssComponent"
    - "Token table can display and edit pattern tokens without breaking existing scalar types"
  artifacts:
    - path: "src/types/token.types.ts"
      provides: "PATTERN_TOKEN_TYPES or export helper isPatternTokenType()"
      contains: "cssClass|htmlTemplate|htmlCssComponent"
---

<objective>
Add **three** table-level token types and a **single object value shape** per `999.1-CONTEXT.md` D-01–D-04: `{ name, body, css? }`. Wire the **type** column and **value** editors in the generator/table so users can create and edit pattern rows. No graph work in this plan.

Purpose: Data model + UX foundation before graph nodes and export filtering.
Output: Extended `TokenType` / `TOKEN_TYPES`, type guards, object editors for value cells, safe defaults for new tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.1-css-html-pattern-token-types/999.1-CONTEXT.md
@.planning/phases/999.1-css-html-pattern-token-types/999.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + helpers</name>
  <files>src/types/token.types.ts</files>
  <action>
1. Add to `TokenType` union: `cssClass`, `htmlTemplate`, `htmlCssComponent`.
2. Append to `TOKEN_TYPES` in a sensible order (e.g. after `typography` or in a small “Pattern” comment block).
3. Export `PatternTokenValue` interface: `{ name: string; body: string; css?: string }`.
4. Export `isPatternTokenType(t: TokenType): boolean` and `PATTERN_TOKEN_TYPES` readonly array for reuse in export + badge (Plan 03).
5. Add `normalizePatternValue(raw: unknown): PatternTokenValue` — coerce legacy string/empty to `{ name: '', body: '' }`.
  </action>
</task>

<task type="auto">
  <name>Task 2: TokenGeneratorForm value UI</name>
  <files>src/components/tokens/TokenGeneratorForm.tsx</files>
  <read_first>
    - TokenGeneratorForm.tsx — value column branches for string/number/color
    - parseTokenValue / stringify patterns used elsewhere on the page
  </read_first>
  <action>
When `token.type` is a pattern type:
- Show **name** and **body** fields (inputs or textareas — `body` may be multiline).
- For `htmlCssComponent` only, show optional **css** textarea.
- On save, set `token.value` to `PatternTokenValue` object.
- Read `parseValue` / `getDisplayValue` paths so table display does not show `[object Object]`.
- Ensure bulk **change type** and theme overlay paths do not corrupt object values (narrow fixes only).
  </action>
</task>

<task type="auto">
  <name>Task 3: TokenTable / display</name>
  <files>src/components/tokens/TokenTable.tsx</files>
  <action>
If TokenTable renders raw `value` for non-edit mode, add branches for pattern types: show abbreviated summary (e.g. `name` + truncated `body`) or structured preview consistent with TokenGeneratorForm.
  </action>
</task>

<task type="human">
  <name>Human verify</name>
  <action>
Create a token of each pattern type in the UI; reload collection; confirm values persist as objects in MongoDB.
  </action>
</task>

</tasks>
