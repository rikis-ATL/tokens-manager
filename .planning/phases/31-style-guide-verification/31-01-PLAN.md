---
phase: 31-style-guide-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VERIFY-25]

must_haves:
  truths:
    - "Existing Jest/RTL tests for Style Guide components pass with exit code 0"
    - "Existing Jest/RTL tests for filterGroupsForActiveTheme pass with exit code 0"
    - "No regressions introduced by Phases 26-30 in Style Guide automated test coverage"
  artifacts: []
  key_links: []
---

<objective>
Run existing automated tests for Phase 25 Style Guide components and the filterGroupsForActiveTheme utility to confirm the automated baseline still passes. This proves no code changes from Phases 26-30 broke Style Guide behavior before the manual browser verification begins.

Purpose: Establish automated green baseline as prerequisite for browser UAT
Output: Test run confirmation (exit 0) — no files created or modified
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-style-guide-verification/31-CONTEXT.md
@.planning/phases/25-enhance-read-only-view-of-token-collections/25-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Style Guide automated test suite</name>
  <files></files>
  <read_first>
    src/components/tokens/style-guide/__tests__/styleGuidePreviews.test.tsx
    src/utils/__tests__/filterGroupsForActiveTheme.test.ts
  </read_first>
  <action>
Run the two test files that cover Phase 25 Style Guide functionality:

1. Run: `yarn jest src/components/tokens/style-guide/__tests__/styleGuidePreviews.test.tsx src/utils/__tests__/filterGroupsForActiveTheme.test.ts --verbose`

2. Verify both test suites pass with exit code 0.

3. If any test fails:
   - Read the failing test and the source file it tests
   - Determine if the failure is a regression from Phases 26-30 or a pre-existing issue
   - Document the failure details (test name, error message, source file) for the SUMMARY

4. If all tests pass, record the test count and suite names for the SUMMARY.

These tests cover:
- styleGuidePreviews.test.tsx: ColorPaletteRow tooltips (D-07), SpacingPreview bar width (D-08), TypographySpecimen styles (D-09), ShadowPreview box-shadow (D-10), BorderRadiusPreview border-radius (D-11), StyleGuidePanel re-render on prop change (D-06 proxy)
- filterGroupsForActiveTheme.test.ts: Disabled group exclusion (D-05)
  </action>
  <verify>
    <automated>cd /Users/user/dev/tokens-manager && yarn jest src/components/tokens/style-guide/__tests__/styleGuidePreviews.test.tsx src/utils/__tests__/filterGroupsForActiveTheme.test.ts --verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - yarn jest exits with code 0
    - Output contains "Tests:" line showing all tests passed (0 failed)
    - Output contains "Test Suites:" line showing 2 suites passed
  </acceptance_criteria>
  <done>Both test suites pass with exit code 0, confirming no regressions from Phases 26-30 in Style Guide automated coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply — this plan runs read-only automated tests with no user input, no API calls, and no file modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | N/A | Test runner | accept | Read-only test execution; no attack surface |
</threat_model>

<verification>
- `yarn jest` exits 0 for both test files
- No test failures reported
</verification>

<success_criteria>
- Both Style Guide test suites pass (0 failures)
- Automated baseline confirmed green before browser UAT
</success_criteria>

<output>
After completion, create `.planning/phases/31-style-guide-verification/31-01-SUMMARY.md`
</output>
