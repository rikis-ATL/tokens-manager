---
phase: 31-style-guide-verification
plan: 03
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - .planning/phases/31-style-guide-verification/31-HUMAN-UAT.md
autonomous: false
requirements: [VERIFY-25]
must_haves:
  truths:
    - "Human has verified the Style Guide tab in a real browser session"
    - "All 8 checklist items from 31-BROWSER-CHECKLIST.md have a pass/fail result"
    - "Results are documented in 31-HUMAN-UAT.md matching Phase 25 UAT format"
    - "If any regression found, it is fixed and re-verified before sign-off"
  artifacts:
    - path: ".planning/phases/31-style-guide-verification/31-HUMAN-UAT.md"
      provides: "Browser UAT results for Phase 25 Style Guide verification"
      contains: "## Tests"
  key_links:
    - from: "31-BROWSER-CHECKLIST.md"
      to: "31-HUMAN-UAT.md"
      via: "Checklist items become UAT test entries"
      pattern: "result: (approved|failed)"
---

<objective>
Guide the human through a browser verification session of the Phase 25 Style Guide tab, working through all 8 checklist items. Document results in 31-HUMAN-UAT.md using the same format as Phase 25's HUMAN-UAT.md. If regressions are found, fix them inline per D-06 and re-verify.

Purpose: Complete the formal browser smoke test that Phase 25 VERIFICATION.md recommended, satisfying VERIFY-25
Output: `.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md` with pass/fail for all 8 items
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-style-guide-verification/31-CONTEXT.md
@.planning/phases/31-style-guide-verification/31-BROWSER-CHECKLIST.md
@.planning/phases/25-enhance-read-only-view-of-token-collections/25-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and prepare UAT document skeleton</name>
  <files>.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/25-enhance-read-only-view-of-token-collections/25-HUMAN-UAT.md
    .planning/phases/31-style-guide-verification/31-BROWSER-CHECKLIST.md
  </read_first>
  <action>
1. Start the dev server in background: `yarn dev` (if not already running).

2. Create `.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md` with this exact structure (matching the Phase 25 HUMAN-UAT.md format):

Frontmatter (wrapped in triple-dash delimiters):
- status: in-progress
- phase: 31-style-guide-verification
- source: [31-BROWSER-CHECKLIST.md, 25-VERIFICATION.md]
- started: {current ISO timestamp}
- updated: {current ISO timestamp}

Then 8 test sections following the Phase 25 format:

## Current Test

Awaiting browser verification

## Tests

### 1. Color swatch rendering and hover tooltips
expected: Horizontal palette row of colored swatches per color group. Hovering a swatch shows tooltip with `token.path: resolvedHex`. Swatch background matches resolved color value. (D-07)
result: pending

### 2. Spacing bar proportions and labels
expected: Grey bars with proportional widths corresponding to spacing values (capped at 300px). Label shows resolved value. (D-08)
result: pending

### 3. Typography font styles applied to specimen
expected: Sample text rendered with token font properties (font-family, font-size, font-weight, line-height). Token name visible alongside specimen. (D-09)
result: pending

### 4. Shadow preview tiles
expected: 30x30 div with box-shadow applied per token value. Token name as label. Shadow visually distinguishable. (D-10)
result: pending

### 5. Border-radius preview tiles
expected: 30x30 div with border-radius applied per token value. Token name as label. Different values produce visibly different rounding. (D-11)
result: pending

### 6. Other/fallback token cards
expected: Non-visual token types render as name+value text cards. No tokens hidden — every token appears. (D-12)
result: pending

### 7. Theme switching updates Style Guide values
expected: Switching themes causes Style Guide to re-render with theme-overridden values. Color swatches change, spacing bars resize as appropriate. Immediate, no reload. (D-06)
result: pending

### 8. Disabled groups hidden from Style Guide
expected: Tokens from disabled group do not appear in Style Guide when theme with disabled group is active. Switching back to Default shows all groups. (D-05)
result: pending

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0
blocked: 0

## Gaps

(initially empty)
  </action>
  <verify>
    <automated>test -f /Users/user/dev/tokens-manager/.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md && grep -c "result: pending" /Users/user/dev/tokens-manager/.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md`
    - File contains frontmatter with `status: in-progress` and `phase: 31-style-guide-verification`
    - File contains "## Tests" section
    - File contains 8 test entries each with "result: pending"
    - File contains "## Summary" with "total: 8" and "pending: 8"
    - File contains "## Gaps" section (empty initially)
    - `yarn dev` process is running (or was confirmed running)
  </acceptance_criteria>
  <done>UAT document skeleton exists with 8 pending test items, dev server is running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification session</name>
  <files></files>
  <read_first>
    .planning/phases/31-style-guide-verification/31-HUMAN-UAT.md
    .planning/phases/31-style-guide-verification/31-BROWSER-CHECKLIST.md
  </read_first>
  <what-built>
The dev server is running and the UAT document is prepared. The user needs to perform a browser verification of the Style Guide tab.
  </what-built>
  <how-to-verify>
1. Open browser to http://localhost:3000
2. Navigate to a collection that has diverse token types (color, spacing, typography, shadow, border-radius, and at least one other type)
3. Click the "Style Guide" tab
4. Work through items 1-6 on the Default theme:
   - Item 1: Check color swatch rows and hover tooltips
   - Item 2: Check spacing grey bars with proportional width
   - Item 3: Check typography specimen text with applied font styles
   - Item 4: Check shadow preview 30x30 tiles
   - Item 5: Check border-radius preview 30x30 tiles
   - Item 6: Check that other/unknown token types show as name+value cards
5. If the collection has themes, work through items 7-8:
   - Item 7: Switch themes and verify Style Guide values update
   - Item 8: If a theme has a disabled group, verify those tokens are hidden
6. For each item, report: "approved" or describe the issue observed

Report all 8 results (or note "skipped -- no themes available" for items 7-8 if the collection has no themes).
  </how-to-verify>
  <action>Present the checklist to the user and wait for their browser verification results. Do not proceed until all 8 items have a result.</action>
  <verify>
    <automated>echo "Checkpoint: human verification required"</automated>
  </verify>
  <resume-signal>Report results for all 8 items (e.g., "1: approved, 2: approved, 3: issue -- font not applied, ...") or type "all approved"</resume-signal>
  <acceptance_criteria>
    - Human has provided pass/fail result for all 8 checklist items (or justified skip for items 7-8)
    - Results are captured in the checkpoint response
  </acceptance_criteria>
  <done>Human has reported pass/fail for all 8 browser verification items</done>
</task>

<task type="auto">
  <name>Task 3: Record UAT results and finalize document</name>
  <files>.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/31-style-guide-verification/31-HUMAN-UAT.md
  </read_first>
  <action>
Update `.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md` based on the human's reported results from Task 2:

1. Update each test entry's `result:` field from `pending` to:
   - `approved` if the item passed
   - `failed -- {description of issue}` if the item failed
   - `skipped -- {reason}` if the item could not be tested

2. Update the `## Current Test` section to: "Approved by user {date}" (if all passed) or "Issues found -- see Gaps" (if any failed)

3. Update the `## Summary` section with final counts:
   - total: 8
   - passed: {count of approved}
   - issues: {count of failed}
   - pending: 0
   - skipped: {count of skipped}
   - blocked: 0

4. Update the frontmatter:
   - `status: passed` (if all approved/skipped) or `status: failed` (if any issues)
   - `updated: {current ISO timestamp}`

5. If any items failed, add the regression details under `## Gaps` with:
   - Which item failed
   - What was observed vs expected
   - Which source file is likely responsible (reference the component files from 31-CONTEXT.md canonical_refs)

6. If regressions were found (per D-06), note that a gap-closure plan is needed.
  </action>
  <verify>
    <automated>grep -c "result: pending" /Users/user/dev/tokens-manager/.planning/phases/31-style-guide-verification/31-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep for "result: pending" returns 0 (no pending items remain)
    - File contains "status: passed" or "status: failed" in frontmatter (not "in-progress")
    - Summary section total equals 8
    - Summary passed + issues + skipped equals 8
    - If status is "failed", the Gaps section contains at least one entry
  </acceptance_criteria>
  <done>31-HUMAN-UAT.md is complete with all 8 test results recorded, summary tallied, and status finalized. If regressions found, gaps documented for potential gap-closure plan per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply — this plan creates a documentation file and runs a dev server locally. No user-facing API changes, no auth changes, no data mutations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | N/A | Dev server + docs | accept | Local dev server only; documentation file has no security surface |
</threat_model>

<verification>
- 31-HUMAN-UAT.md exists with all 8 items resolved (no pending)
- Status is either "passed" (all approved) or "failed" (with gaps documented)
- Format matches Phase 25 HUMAN-UAT.md structure
</verification>

<success_criteria>
- Human has completed browser verification of all 8 Style Guide areas
- Results documented in 31-HUMAN-UAT.md with pass/fail for each item
- If all passed: VERIFY-25 is satisfied, Phase 31 complete
- If any failed: Gaps documented, gap-closure plan needed per D-06
</success_criteria>

<output>
After completion, create `.planning/phases/31-style-guide-verification/31-03-SUMMARY.md`
</output>
