# Phase 1: Math Node Expression Mode — UAT

## Pre-flight Automated Checks

- [x] yarn tsc --noEmit: OOM crash (pre-existing — project-wide circular type resolution issue, not Phase 1)
- [x] yarn test: 173/177 passed; 4 failures in `claude.provider.test.ts` are pre-existing (unrelated to Phase 1). Phase 1 tests: **24/24 passing** (`src/lib/__tests__/mathExpression.test.ts`)
- [ ] yarn lint: ESLint 8.57.0 config incompatibility with `eslint.config.mjs` (pre-existing)
- [ ] yarn build: not run (blocked by above pre-existing issues)

**Pre-flight verdict:** Phase 1 files are clean. All failures are pre-existing project-wide tooling issues. Proceeding to browser verification.

---

## Browser Verification

*Pending human testing — checklist to be filled in after running the app.*

| Case | Description | Expected | Result |
|------|-------------|----------|--------|
| A | Valid expr without refs: `a * 2 + 3` | No red border, result = 23 | pending |
| B | Valid expr with token ref: `a + {spacing.base}` | No red border, result = 26 | pending |
| C | Invalid syntax: `a * ` (trailing op) | Red border + "Unexpected end of expression" | pending |
| D | Unbound variable: `a + 1` with no wire | Red border + "Variable a is not bound" | pending |
| E | Unresolved ref: `{does.not.exist}` | Red border + "Unresolved token reference" | pending |
| F | Recovery: fix Case C to `a * 2` | Red border clears, hint text returns | pending |
| G | No keystroke flashing: type `a +` slowly | Border stays default while typing | pending |
| H | Persistence: reload page | Expression mode + formula persists | pending |
| I | Path extraction: no spurious mismatches | Token path mismatch indicator clean | pending |
