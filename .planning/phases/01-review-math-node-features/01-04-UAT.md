# Phase 1: Math Node Expression Mode — UAT

## Pre-flight Automated Checks

- [x] yarn tsc --noEmit: **PASS** (requires `--max-old-space-size=8192`; default heap too small — pre-existing project tooling issue, not a type error)
- [x] yarn test: 174/179 passed; 4 failures in `claude.provider.test.ts` are pre-existing (confirmed on clean branch, unrelated to Phase 1). Phase 1 tests: **24/24 passing** (`src/lib/__tests__/mathExpression.test.ts`). Note: one test was failing (`evaluateExpression` returning 0 instead of null for unresolved refs without a resolver) — fixed in Task 1 pre-flight.
- [ ] yarn lint: ESLint 8.57.0 config incompatibility with `eslint.config.mjs` (pre-existing, confirmed on clean branch)
- [x] yarn build: **PASS** (Next.js production build complete, exit 0)

**Pre-flight verdict:** Phase 1 files are clean. The mathExpression test fix is correct. Lint failure is a pre-existing tooling issue unrelated to Phase 1. Proceeding to browser verification.

---

## Browser Verification

*Pending human testing — checklist to be filled in after running the app.*

| Case | Description | Expected | Result |
|------|-------------|----------|--------|
| A | Valid expr without refs: `a * 2 + 3` | No red border, result = 23 | PASS |
| B | Valid expr with token ref: `a + {spacing.base}` | No red border, result = 26 | PASS |
| C | Invalid syntax: `a * ` (trailing op) | Red border + "Unexpected end of expression" | PASS |
| D | Unbound variable: `a + 1` with no wire | Red border + "Variable a is not bound" | PASS |
| E | Unresolved ref: `{does.not.exist}` | Red border + "Unresolved token reference" | PASS |
| F | Recovery: fix Case C to `a * 2` | Red border clears, hint text returns | PASS |
| G | No keystroke flashing: type `a +` slowly | Border stays default while typing | PASS |
| H | Persistence: reload page | Expression mode + formula persists | PASS |
| I | Path extraction: no spurious mismatches | Token path mismatch indicator clean | PASS |

**Browser verification verdict: ALL CASES PASSED** — approved by user on 2026-04-16
