---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/theme.types.ts
  - src/utils/tokenScope.ts
  - src/utils/resolveActiveThemeForGroup.ts
  - src/lib/themeTokenMerge.ts
  - src/utils/filterGroupsForActiveTheme.ts
  - src/utils/__tests__/tokenScope.test.ts
  - src/utils/__tests__/themeTokenMerge.test.ts
  - src/utils/__tests__/resolveActiveThemeForGroup.test.ts
autonomous: true
requirements:
  - SPEC-GOAL-1
  - SPEC-GOAL-4

must_haves:
  truths:
    - "ITheme has kind: 'color' | 'density' field; colorMode is optional"
    - "COLOR_SCOPE_TYPES and DENSITY_SCOPE_TYPES arrays exist and classify all TokenType values"
    - "dominantScopeForTokenTypes() returns 'color', 'density', or null for a list of types"
    - "resolveActiveThemeIdForGroup() returns the correct theme ID based on a group's dominant type"
    - "mergeDualThemeTokens() merges collection default → color overrides → density overrides"
    - "filterGroupsForDualThemes() filters groups visible under either active theme"
    - "All unit tests pass (yarn test --testPathPattern='tokenScope|themeTokenMerge|resolveActiveTheme')"
  artifacts:
    - path: "src/types/theme.types.ts"
      provides: "ThemeKind type + updated ITheme interface"
      contains: "ThemeKind"
    - path: "src/utils/tokenScope.ts"
      provides: "Scope classification constants and helpers"
      exports: ["COLOR_SCOPE_TYPES", "DENSITY_SCOPE_TYPES", "isColorScopeType", "isDensityScopeType", "dominantScopeForTokenTypes"]
    - path: "src/utils/resolveActiveThemeForGroup.ts"
      provides: "Graph routing utility"
      exports: ["resolveActiveThemeIdForGroup"]
    - path: "src/lib/themeTokenMerge.ts"
      provides: "Dual-merge function"
      exports: ["mergeDualThemeTokens"]
    - path: "src/utils/filterGroupsForActiveTheme.ts"
      provides: "Dual-theme group filter"
      exports: ["filterGroupsForDualThemes"]
  key_links:
    - from: "src/utils/tokenScope.ts"
      to: "src/types/token.types.ts"
      via: "import type { TokenType }"
      pattern: "import type.*TokenType.*token.types"
    - from: "src/utils/resolveActiveThemeForGroup.ts"
      to: "src/utils/tokenScope.ts"
      via: "dominantScopeForTokenTypes"
      pattern: "dominantScopeForTokenTypes"
    - from: "src/lib/themeTokenMerge.ts"
      to: "src/utils/tokenScope.ts"
      via: "COLOR_SCOPE_TYPES, DENSITY_SCOPE_TYPES"
      pattern: "COLOR_SCOPE_TYPES.*DENSITY_SCOPE_TYPES"
---

<objective>
Establish the type, utility, and merge layer that every other plan in this phase depends on. No UI changes — pure TypeScript foundations.

Purpose: Downstream plans (02, 03, 04) need ThemeKind, scope classification, the dual-merge function, and the graph-routing utility before they can be implemented.
Output: 5 modified/new source files + 3 test files, all covered by automated unit tests.
</objective>

<execution_context>
@/Users/user/dev/tokens-manager/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/dev/tokens-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/user/dev/tokens-manager/.planning/PROJECT.md
@/Users/user/dev/tokens-manager/.planning/ROADMAP.md
@/Users/user/dev/tokens-manager/.planning/STATE.md
@/Users/user/dev/tokens-manager/.planning/phases/33-theme-configuration-color-density/33-SPEC.md
@/Users/user/dev/tokens-manager/.planning/phases/33-theme-configuration-color-density/33-CONTEXT.md
@/Users/user/dev/tokens-manager/.planning/phases/33-theme-configuration-color-density/33-PATTERNS.md

<interfaces>
<!-- Current ITheme (src/types/theme.types.ts) -->
```typescript
export type ColorMode = 'light' | 'dark';
export type ThemeGroupState = 'disabled' | 'enabled' | 'source';

export interface ITheme {
  id: string;
  name: string;
  colorMode: ColorMode;           // currently required
  groups: Record<string, ThemeGroupState>;
  tokens: TokenGroup[];
  graphState?: CollectionGraphState | null;
}
```

<!-- Current token types (src/types/token.types.ts lines 1-74) -->
```typescript
export type TokenType =
  | 'color' | 'dimension' | 'fontFamily' | 'fontWeight' | 'fontSize'
  | 'lineHeight' | 'letterSpacing' | 'borderRadius' | 'borderWidth'
  | 'opacity' | 'boxShadow' | 'textShadow' | 'duration' | 'cubicBezier'
  | 'number' | 'string' | 'strokeStyle' | 'border' | 'transition'
  | 'shadow' | 'gradient' | 'typography' | 'cssClass' | 'htmlTemplate' | 'htmlCssComponent';

export const TOKEN_TYPES: TokenType[] = ['color', 'dimension', ...]; // all 25 types

export const PATTERN_TOKEN_TYPES: TokenType[] = ['cssClass', 'htmlTemplate', 'htmlCssComponent'];

export function isPatternTokenType(t: TokenType): boolean { return PATTERN_TOKEN_TYPES.includes(t); }
```

<!-- Current themeTokenMerge.ts signature -->
```typescript
export function mergeThemeTokens(
  masterTokens: Record<string, unknown>,
  theme: ITheme | null,
  namespace: string
): Record<string, unknown>
```

<!-- Current filterGroupsForActiveTheme.ts -->
```typescript
export function filterGroupsForActiveTheme(
  masterGroups: TokenGroup[],
  activeTheme: ITheme | null | undefined,
): TokenGroup[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Create test stubs + update ITheme with ThemeKind</name>
  <files>
    src/types/theme.types.ts
    src/utils/__tests__/tokenScope.test.ts
    src/utils/__tests__/themeTokenMerge.test.ts
    src/utils/__tests__/resolveActiveThemeForGroup.test.ts
  </files>
  <read_first>
    - src/types/theme.types.ts (current file — read before modifying)
    - src/types/token.types.ts (lines 1-74 — pattern for union type + export const array)
    - src/utils/__tests__/ (check if directory and any existing tests exist)
  </read_first>
  <behavior>
    Test stubs (failing until Tasks 2–3 create the implementations):
    tokenScope.test.ts:
    - isColorScopeType('color') === true
    - isColorScopeType('gradient') === true
    - isColorScopeType('dimension') === false
    - isDensityScopeType('dimension') === true
    - isDensityScopeType('fontSize') === true
    - isDensityScopeType('fontWeight') === true
    - isDensityScopeType('borderRadius') === true
    - isDensityScopeType('borderWidth') === true
    - isDensityScopeType('color') === false
    - dominantScopeForTokenTypes(['color', 'color', 'dimension']) === 'color' (majority)
    - dominantScopeForTokenTypes(['dimension', 'fontSize']) === 'density'
    - dominantScopeForTokenTypes(['string', 'number']) === null (no scope types)
    - dominantScopeForTokenTypes([]) === null
    - COLOR_SCOPE_TYPES includes 'color' and 'gradient'
    - DENSITY_SCOPE_TYPES includes 'dimension', 'fontSize', 'fontWeight', 'borderRadius', 'borderWidth'

    themeTokenMerge.test.ts:
    - mergeDualThemeTokens(masterTokens, null, null, 'token') returns collection default output
    - color theme overrides color tokens but not dimension tokens
    - density theme overrides dimension tokens but not color tokens
    - both themes active: color overrides apply first, density overrides apply second
    - out-of-scope tokens in theme are silently ignored (not merged)
    - (use minimal fixture data: one color group, one dimension group, two tokens each)

    resolveActiveThemeForGroup.test.ts:
    - resolveActiveThemeIdForGroup(colorGroup, 'cThemeId', 'dThemeId') === 'cThemeId'
    - resolveActiveThemeIdForGroup(densityGroup, 'cThemeId', 'dThemeId') === 'dThemeId'
    - resolveActiveThemeIdForGroup(null, 'cThemeId', 'dThemeId') === null
    - resolveActiveThemeIdForGroup(mixedGroup, 'cThemeId', null) === 'cThemeId' (dominant)
    - resolveActiveThemeIdForGroup(colorGroup, null, 'dThemeId') === null (no color theme active)
  </behavior>
  <action>
    1. Read src/types/theme.types.ts in full (15 lines — already in context above).

    2. Update src/types/theme.types.ts:
       - Add `export type ThemeKind = 'color' | 'density';` before the ITheme interface
       - Add `kind: ThemeKind;` field to ITheme (after `name`)
       - Change `colorMode: ColorMode;` to `colorMode?: ColorMode;` (optional — density themes have no colorMode)
       - Keep all other fields unchanged

    3. Create src/utils/__tests__/ directory if absent.

    4. Create src/utils/__tests__/tokenScope.test.ts with all the behavior cases above.
       Import path: `import { ... } from '@/utils/tokenScope';`
       These tests MUST fail initially (file does not exist yet — `passWithNoTests` handles this).

    5. Create src/utils/__tests__/themeTokenMerge.test.ts.
       Import path: `import { mergeDualThemeTokens } from '@/lib/themeTokenMerge';`
       Use minimal fixtures — TokenGroup with 2 color tokens and TokenGroup with 2 dimension tokens.
       Wrap master tokens in namespace key to match the real `processImportedTokens` input format.
       Expect `mergeDualThemeTokens` to be importable once Task 2 adds it.

    6. Create src/utils/__tests__/resolveActiveThemeForGroup.test.ts.
       Import path: `import { resolveActiveThemeIdForGroup } from '@/utils/resolveActiveThemeForGroup';`
       For fixtures: construct minimal TokenGroup objects with tokens array containing types.

    Do NOT create the implementation files yet — only types + test stubs.
  </action>
  <verify>
    <automated>yarn test --testPathPattern="tokenScope|themeTokenMerge|resolveActiveTheme" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/types/theme.types.ts exports ThemeKind and ITheme has `kind: ThemeKind` and `colorMode?: ColorMode`
    - 3 test files exist under src/utils/__tests__/
    - Test runner exits 0 (passWithNoTests — implementations missing, tests skipped not errored)
    - grep confirms: `grep -n "ThemeKind\|kind: ThemeKind\|colorMode\?" src/types/theme.types.ts` shows all 3
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tokenScope.ts, resolveActiveThemeForGroup.ts, extend filterGroupsForActiveTheme.ts + themeTokenMerge.ts</name>
  <files>
    src/utils/tokenScope.ts
    src/utils/resolveActiveThemeForGroup.ts
    src/utils/filterGroupsForActiveTheme.ts
    src/lib/themeTokenMerge.ts
  </files>
  <read_first>
    - src/types/token.types.ts (full file — TokenType union, TOKEN_TYPES array, isPatternTokenType pattern)
    - src/utils/filterGroupsForActiveTheme.ts (full file — sibling function pattern)
    - src/lib/themeTokenMerge.ts (full file — existing mergeThemeTokens, imports, tokenService usage)
    - src/utils/__tests__/tokenScope.test.ts (test expectations to satisfy)
    - src/utils/__tests__/themeTokenMerge.test.ts (test expectations to satisfy)
    - src/utils/__tests__/resolveActiveThemeForGroup.test.ts (test expectations to satisfy)
  </read_first>
  <behavior>
    All behavior from Task 1 test stubs must now pass GREEN.
  </behavior>
  <action>
    1. Create src/utils/tokenScope.ts with exact content:
    ```typescript
    import type { TokenType } from '@/types/token.types';

    /** Token types that belong to the color scope (overridden by color themes) */
    export const COLOR_SCOPE_TYPES: readonly TokenType[] = ['color', 'gradient'] as const;

    /**
     * Token types that belong to the density scope (overridden by density themes).
     * Covers the full range of sizing and typographic scale tokens per CONTEXT.md D-05/D-06.
     */
    export const DENSITY_SCOPE_TYPES: readonly TokenType[] = [
      'dimension',
      'fontSize',
      'fontWeight',
      'borderRadius',
      'borderWidth',
    ] as const;

    export function isColorScopeType(t: TokenType): boolean {
      return (COLOR_SCOPE_TYPES as readonly string[]).includes(t);
    }

    export function isDensityScopeType(t: TokenType): boolean {
      return (DENSITY_SCOPE_TYPES as readonly string[]).includes(t);
    }

    /**
     * Returns 'color' | 'density' | null based on majority token type in a list.
     * Tie goes to 'color'. Empty or all-unscoped list returns null.
     * Per CONTEXT.md D-08.
     */
    export function dominantScopeForTokenTypes(types: TokenType[]): 'color' | 'density' | null {
      const colorCount = types.filter(isColorScopeType).length;
      const densityCount = types.filter(isDensityScopeType).length;
      if (colorCount === 0 && densityCount === 0) return null;
      return colorCount >= densityCount ? 'color' : 'density';
    }
    ```

    2. Create src/utils/resolveActiveThemeForGroup.ts with exact content:
    ```typescript
    import type { TokenGroup } from '@/types/token.types';
    import { dominantScopeForTokenTypes } from '@/utils/tokenScope';

    /**
     * Returns the correct active theme ID for graph state reads/writes,
     * based on the dominant token type in the given group (CONTEXT.md D-08).
     *
     * - Color-dominant group → activeColorThemeId
     * - Density-dominant group → activeDensityThemeId
     * - Mixed equal (tie) → 'color' (color wins tie)
     * - Null/unscoped group → null (collection default)
     */
    export function resolveActiveThemeIdForGroup(
      group: TokenGroup | null | undefined,
      activeColorThemeId: string | null,
      activeDensityThemeId: string | null,
    ): string | null {
      if (!group) return null;
      const allTypes = group.tokens.map(t => t.type);
      const scope = dominantScopeForTokenTypes(allTypes);
      if (scope === 'color') return activeColorThemeId;
      if (scope === 'density') return activeDensityThemeId;
      return null; // unscoped group → collection default
    }
    ```

    3. Extend src/utils/filterGroupsForActiveTheme.ts — add `filterGroupsForDualThemes` as a sibling
       function BELOW the existing `filterGroupsForActiveTheme` function. Do NOT modify the existing
       function. Exact addition:
    ```typescript
    /**
     * Dual-theme variant: a group is visible if it is enabled or source in EITHER active theme.
     * When both themes are null, returns masterGroups unchanged (collection default view).
     * Per CONTEXT.md D-03/D-04.
     */
    export function filterGroupsForDualThemes(
      masterGroups: TokenGroup[],
      colorTheme: ITheme | null | undefined,
      densityTheme: ITheme | null | undefined,
    ): TokenGroup[] {
      if (!colorTheme && !densityTheme) return masterGroups;

      function filterGroups(groups: TokenGroup[]): TokenGroup[] {
        return groups
          .filter((g) => {
            const colorState = colorTheme?.groups[g.id] ?? 'disabled';
            const densityState = densityTheme?.groups[g.id] ?? 'disabled';
            return colorState !== 'disabled' || densityState !== 'disabled';
          })
          .map((g) => ({
            ...g,
            children: g.children ? filterGroups(g.children) : undefined,
          }));
      }

      return filterGroups(masterGroups);
    }
    ```

    4. Extend src/lib/themeTokenMerge.ts — add imports and `mergeDualThemeTokens` BELOW the
       existing `mergeThemeTokens` function. Do NOT modify the existing function.

       Add to imports at top of file:
       ```typescript
       import { COLOR_SCOPE_TYPES, DENSITY_SCOPE_TYPES } from '@/utils/tokenScope';
       import type { TokenType } from '@/types/token.types';
       ```

       Add new functions below existing mergeThemeTokens:
       ```typescript
       /**
        * Internal: apply a theme's token overrides to a group tree,
        * scoped to the given token types. Groups are replaced only if
        * they contain at least one token of a scoped type.
        */
       function applyThemeOverrides(
         masterGroups: TokenGroup[],
         theme: ITheme,
         scopeTypes: readonly TokenType[],
       ): TokenGroup[] {
         const themeGroupMap = new Map<string, TokenGroup>(theme.tokens.map(g => [g.id, g]));
         return masterGroups.map(masterGroup => {
           const state = theme.groups[masterGroup.id] ?? 'disabled';
           if (state === 'disabled' || state === 'source') return masterGroup;
           const hasScoped = masterGroup.tokens.some(
             t => (scopeTypes as readonly string[]).includes(t.type)
           );
           if (!hasScoped) return masterGroup;
           return themeGroupMap.get(masterGroup.id) ?? masterGroup;
         });
       }

       /**
        * Three-way merge: collection default → color theme overrides → density theme overrides.
        * Each theme only overrides tokens within its own type scope.
        * When both themes are null, returns collection default tokens unchanged.
        * Per CONTEXT.md D-04.
        */
       export function mergeDualThemeTokens(
         masterTokens: Record<string, unknown>,
         colorTheme: ITheme | null,
         densityTheme: ITheme | null,
         namespace: string,
       ): Record<string, unknown> {
         if (!colorTheme && !densityTheme) {
           const { groups } = tokenService.processImportedTokens(masterTokens, namespace);
           return tokenService.generateStyleDictionaryOutput(groups, namespace, true);
         }
         const { groups: masterGroups } = tokenService.processImportedTokens(masterTokens, namespace);
         const afterColor = colorTheme
           ? applyThemeOverrides(masterGroups, colorTheme, COLOR_SCOPE_TYPES)
           : masterGroups;
         const afterDensity = densityTheme
           ? applyThemeOverrides(afterColor, densityTheme, DENSITY_SCOPE_TYPES)
           : afterColor;
         return tokenService.generateStyleDictionaryOutput(afterDensity, namespace, true);
       }
       ```

    Run tests after each file to confirm GREEN before moving to the next.
  </action>
  <verify>
    <automated>yarn test --testPathPattern="tokenScope|themeTokenMerge|resolveActiveTheme" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - yarn test --testPathPattern exits 0 with all tests PASS (no failures)
    - grep confirms tokenScope.ts exports: `grep "export" src/utils/tokenScope.ts`
    - grep confirms resolveActiveThemeForGroup.ts exports: `grep "export function" src/utils/resolveActiveThemeForGroup.ts`
    - grep confirms filterGroupsForDualThemes added: `grep "filterGroupsForDualThemes" src/utils/filterGroupsForActiveTheme.ts`
    - grep confirms mergeDualThemeTokens added: `grep "mergeDualThemeTokens" src/lib/themeTokenMerge.ts`
    - TypeScript compiles without errors on changed files: `yarn tsc --noEmit 2>&1 | grep -E "error TS" | head -10`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme.kind at read time | MongoDB documents pre-migration have no `kind` field — read sites must default to 'color' |
| scope trimming | applyThemeOverrides must not leak cross-scope token overrides into the merged output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Tampering | applyThemeOverrides in themeTokenMerge.ts | mitigate | Only replace masterGroup when `hasScoped` check passes — prevents density theme injecting color token values and vice versa |
| T-33-01-02 | Information Disclosure | theme.kind default | mitigate | At every read site use `(theme.kind ?? 'color') as ThemeKind` — absent kind defaults to color per backward-compat contract |
| T-33-01-03 | Denial of Service | dominantScopeForTokenTypes with large group | accept | Pure in-memory array filter; no I/O; negligible cost even for 1000-token groups |
</threat_model>

<verification>
- `yarn test --testPathPattern="tokenScope|themeTokenMerge|resolveActiveTheme"` exits 0, all tests PASS
- `yarn tsc --noEmit` reports zero new TypeScript errors in the 4 changed source files
- `grep -n "ThemeKind\|kind: ThemeKind\|colorMode\?" src/types/theme.types.ts` shows all 3 declarations
- `grep "export" src/utils/tokenScope.ts` shows COLOR_SCOPE_TYPES, DENSITY_SCOPE_TYPES, isColorScopeType, isDensityScopeType, dominantScopeForTokenTypes
- `grep "export function" src/utils/resolveActiveThemeForGroup.ts` shows resolveActiveThemeIdForGroup
- `grep "filterGroupsForDualThemes\|mergeDualThemeTokens" src/utils/filterGroupsForActiveTheme.ts src/lib/themeTokenMerge.ts` confirms both additions
</verification>

<success_criteria>
- ThemeKind type exists; ITheme.kind is required; ITheme.colorMode is optional
- COLOR_SCOPE_TYPES = ['color', 'gradient']; DENSITY_SCOPE_TYPES = ['dimension', 'fontSize', 'fontWeight', 'borderRadius', 'borderWidth']
- All 3 test files green (15+ assertions total)
- No TypeScript compile errors in modified files
- mergeDualThemeTokens correctly applies color overrides before density overrides (verified by test)
</success_criteria>

<output>
After completion, create `.planning/phases/33-theme-configuration-color-density/33-01-SUMMARY.md`
</output>
