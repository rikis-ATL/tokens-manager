---
phase: 33-theme-configuration-color-density
plan: 06
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - src/components/themes/ThemeGroupMatrix.tsx
autonomous: true
requirements: [SPEC-GOAL-3, SPEC-GOAL-4]
gap_closure: true

must_haves:
  truths:
    - "ThemeGroupMatrix renders a row for every group AND every subgroup (recursively flattened)"
    - "Subgroups such as typography/fontSize appear as distinct rows in the matrix table"
    - "Clicking a state button (Disabled / Enabled / Source) on any row — including subgroup rows — persists the change via handleGroupStateChange"
    - "Groups with no children and groups with deeply nested children all appear as flat equal rows — no indentation, no hierarchy"
  artifacts:
    - path: "src/components/themes/ThemeGroupMatrix.tsx"
      provides: "Recursive group flattening; all TokenGroup.children rendered as flat rows"
      contains: "flattenGroups"
  key_links:
    - from: "ThemeGroupMatrix"
      to: "TokenGroup.children"
      via: "flattenGroups recursive util inside ThemeGroupMatrix.tsx"
      pattern: "children"
    - from: "tokens/page.tsx ThemeGroupMatrix"
      to: "handleGroupStateChange"
      via: "onStateChange prop — already wired; no change needed"
      pattern: "onStateChange.*handleGroupStateChange"
---

<objective>
Fix ThemeGroupMatrix to recursively flatten TokenGroup.children so all subgroups appear as
distinct rows in the group-state table.

Purpose: D-18 (CONTEXT.md) — the matrix currently only renders top-level groups. Subgroups
such as `typography/fontSize` are invisible to the user, making it impossible to set their
group state (enabled / source / disabled) on a theme.

Output: Updated ThemeGroupMatrix.tsx with a `flattenGroups` helper that produces a flat
array of all groups and their descendants. The Themes tab two-panel layout and
handleGroupStateChange callback are already in place (confirmed in tokens/page.tsx); this
plan closes the only remaining gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-theme-configuration-color-density/33-CONTEXT.md
@.planning/phases/33-theme-configuration-color-density/33-SPEC.md

<!-- Key facts confirmed by reading source files before writing this plan: -->

<!-- ThemeGroupMatrix.tsx (src/components/themes/ThemeGroupMatrix.tsx):
  - Props: theme: ITheme, groups: TokenGroup[], onStateChange: (groupId, state) => void
  - Line 58: {groups.map((group) => { ... })} — only top-level, zero references to .children
  - Fix: add flattenGroups helper, replace groups.map with flattenGroups(groups).map
  - No other changes needed; table structure, STATE_LABELS, groupScope all correct as-is
-->

<!-- tokens/page.tsx — Themes tab already complete (confirmed):
  - configuringThemeId state at line 250
  - handleGroupStateChange callback at lines 953-973: optimistic update + PUT /api/collections/[id]/themes/[configuringThemeId]
  - Themes TabsContent at lines 1624-1665: left=ThemeList flat, right=ThemeGroupMatrix
  - groups={masterGroups} passed to ThemeGroupMatrix — masterGroups is a tree with .children
  - NO changes to tokens/page.tsx needed
-->

<!-- TokenGroup interface (src/types/token.types.ts line 155-175):
  children?: TokenGroup[]   <-- optional nested children
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flattenGroups helper and fix ThemeGroupMatrix to render all subgroups</name>
  <files>src/components/themes/ThemeGroupMatrix.tsx</files>

  <read_first>
    - src/components/themes/ThemeGroupMatrix.tsx — full file (current state; confirmed 103 lines)
    - src/types/token.types.ts lines 155-175 — TokenGroup interface confirming children?: TokenGroup[]
  </read_first>

  <action>
Add a `flattenGroups` function inside ThemeGroupMatrix.tsx (above the component, after the
`groupScope` helper). Then replace `groups.map(...)` in the JSX with `flattenGroups(groups).map(...)`.

The function signature and implementation:

```typescript
/** Recursively flatten a TokenGroup tree into a flat array (depth-first, parent before children). */
function flattenGroups(groups: TokenGroup[]): TokenGroup[] {
  const result: TokenGroup[] = [];
  for (const group of groups) {
    result.push(group);
    if (group.children && group.children.length > 0) {
      result.push(...flattenGroups(group.children));
    }
  }
  return result;
}
```

In the component JSX, change the single line:
  BEFORE: `{groups.map((group) => {`
  AFTER:  `{flattenGroups(groups).map((group) => {`

No other changes. The empty-state guard at line 33 (`if (groups.length === 0)`) checks the
prop before flattening — keep it as-is (masterGroups being empty means no groups exist at all).

Do NOT add indentation or visual hierarchy. D-18 explicitly requires "flat table, no
indentation" — all rows are equal regardless of nesting depth.

Do NOT change the `groupScope` function — it already reads `group.tokens` correctly.
Group state lookup `theme.groups[group.id]` already works per-group-id at any depth since
IDs are unique across the entire tree.
  </action>

  <verify>
    <automated>grep -n "flattenGroups" /Users/user/dev/tokens-manager/src/components/themes/ThemeGroupMatrix.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `flattenGroups` function is defined in ThemeGroupMatrix.tsx (grep returns 2+ lines: definition + usage)
    - The word `children` appears in ThemeGroupMatrix.tsx (confirms the recursive branch is present)
    - `flattenGroups(groups).map` replaces the original `groups.map` call in the JSX
    - No `indent` or `paddingLeft` style is applied to rows (no visual hierarchy per D-18)
    - `yarn tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>
    ThemeGroupMatrix.tsx renders all groups AND all their descendants as flat rows.
    A collection with groups [colors, typography] where typography has children [fontSize, fontWeight]
    produces 4 rows: colors, typography, fontSize, fontWeight.
    Each row's state button calls onStateChange(group.id, state) — unchanged behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PUT /api/collections/[id]/themes/[themeId] | Group state update; already enforced by existing handleGroupStateChange |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06-01 | Tampering | ThemeGroupMatrix onStateChange | accept | Pure client-side rendering fix; no new trust boundary introduced. The PUT call and auth are handled by handleGroupStateChange in tokens/page.tsx (unchanged). |
</threat_model>

<verification>
After implementation:

1. TypeScript compiles: `yarn tsc --noEmit` exits 0
2. `grep -c "flattenGroups" src/components/themes/ThemeGroupMatrix.tsx` returns at least 2
3. `grep "children" src/components/themes/ThemeGroupMatrix.tsx` returns a match
4. Open the Themes tab in the app; select a theme; confirm subgroups appear as rows (e.g. if a collection has a `typography` group with children `fontSize` and `fontWeight`, all three rows are visible)
5. Clicking Enabled / Source / Disabled on a subgroup row updates the state (no console errors, optimistic UI update visible)
</verification>

<success_criteria>
- ThemeGroupMatrix renders N rows where N = total count of all groups at all depths in masterGroups (not just top-level count)
- State changes on subgroup rows persist via PUT /api/collections/[id]/themes/[themeId] (handleGroupStateChange already calls this)
- No TypeScript errors introduced
- D-18 fully closed: subgroups visible and configurable in the Themes tab matrix
</success_criteria>

<output>
After completion, create `.planning/phases/33-theme-configuration-color-density/33-06-SUMMARY.md`
</output>
