---
phase: 15-multi-row-actions
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/components/tokens/DeleteConfirmDialog.tsx
  - src/components/tokens/GroupPickerModal.tsx
  - src/components/tokens/BulkActionBar.tsx
autonomous: true
requirements: [BULK-02, BULK-03, BULK-04, BULK-05, BULK-06]

must_haves:
  truths:
    - "DeleteConfirmDialog renders a shadcn Dialog with the selected count, Cancel and Delete buttons; Delete button calls onConfirm, Cancel calls onCancel"
    - "GroupPickerModal renders a full recursive group tree as a scrollable list with indentation; clicking a group row calls onSelect(groupId); the source group is visually disabled and not selectable"
    - "BulkActionBar is visible when selectedCount > 0 and hidden when selectedCount === 0"
    - "BulkActionBar shows the selection count label 'N selected'"
    - "BulkActionBar Add Prefix action expands an inline text input with a live preview list (up to 5 token paths, '+N more' for remainder)"
    - "BulkActionBar Remove Prefix action pre-fills the input with detectedPrefix and shows the same live preview"
    - "BulkActionBar Change Type action renders a shadcn Select with all token types"
    - "Escape key within BulkActionBar calls onClearSelection"
  artifacts:
    - path: "src/components/tokens/DeleteConfirmDialog.tsx"
      provides: "Bulk delete confirmation dialog"
      exports: ["DeleteConfirmDialog"]
    - path: "src/components/tokens/GroupPickerModal.tsx"
      provides: "Group tree picker modal for move-to-group"
      exports: ["GroupPickerModal"]
    - path: "src/components/tokens/BulkActionBar.tsx"
      provides: "Floating action bar for multi-row operations"
      exports: ["BulkActionBar"]
  key_links:
    - from: "src/components/tokens/BulkActionBar.tsx"
      to: "src/components/tokens/DeleteConfirmDialog.tsx"
      via: "rendered inside BulkActionBar when delete is triggered"
      pattern: "DeleteConfirmDialog"
    - from: "src/components/tokens/BulkActionBar.tsx"
      to: "src/components/tokens/GroupPickerModal.tsx"
      via: "rendered inside BulkActionBar when move-to-group is triggered"
      pattern: "GroupPickerModal"
---

<objective>
Create three new presentational components: `DeleteConfirmDialog`, `GroupPickerModal`, and `BulkActionBar`. These are pure UI components — they receive all data and callbacks as props, hold minimal local state (inline prefix input values, open/closed modal state), and emit events upward. No bulk mutation logic lives here; that is in `bulkTokenActions.ts`.

Purpose: Isolating these components from the wiring in `TokenGeneratorForm` keeps each file single-responsibility and allows independent testing.

Output: Three new files in `src/components/tokens/`, added to the directory's `index.ts` barrel.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-multi-row-actions/15-CONTEXT.md
@.planning/phases/15-multi-row-actions/15-RESEARCH.md
@src/components/tokens/ClearFormDialog.tsx
@src/components/tokens/TokenGroupTree.tsx
@src/utils/tree.utils.ts
@src/types/token.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeleteConfirmDialog and GroupPickerModal</name>
  <files>
    src/components/tokens/DeleteConfirmDialog.tsx
    src/components/tokens/GroupPickerModal.tsx
  </files>
  <action>
    **DeleteConfirmDialog.tsx**

    Model after `ClearFormDialog.tsx`. Props interface:
    ```typescript
    interface DeleteConfirmDialogProps {
      open: boolean;
      count: number;
      onConfirm: () => void;
      onCancel: () => void;
    }
    ```
    Render a shadcn `Dialog` (controlled by `open`, `onOpenChange` calls `onCancel` on close).
    Body text: `"Delete {count} selected token{count === 1 ? '' : 's'}? This cannot be undone."`
    Footer: `<Button variant="outline" onClick={onCancel}>Cancel</Button>` and `<Button className="bg-red-600 hover:bg-red-700 text-white" onClick={onConfirm}>Delete</Button>`.

    **GroupPickerModal.tsx**

    Props interface:
    ```typescript
    interface GroupPickerModalProps {
      open: boolean;
      groups: TokenGroup[];           // full masterGroups tree
      sourceGroupId: string;          // disable this group in the picker
      onSelect: (groupId: string) => void;
      onCancel: () => void;
    }
    ```
    Render a shadcn `Dialog` with title "Move to group". Body: scrollable `<div className="max-h-64 overflow-y-auto">` containing a flat rendered list of all groups, built using the `flattenTree` utility from `@/utils` (already exists in `groupMove.ts`, exported from `src/utils/index.ts`). Each item is a `<button>` row with `pl-{depth * 4}` indentation (use inline style `paddingLeft: depth * 16` for safety), showing the group's display label (last segment of `group.id.split('/')` or `group.name` — whichever field stores the human-readable name; check `TokenGroup` type). The source group row is rendered as `disabled` with `opacity-50 cursor-not-allowed`. Clicking a non-disabled row calls `onSelect(group.id)`.

    Check `src/types/token.types.ts` to confirm the correct field names on `TokenGroup` (likely `id` and `name`). Use `flattenTree` from `@/utils/tree.utils` (verify export name) or inline a simple recursive flatten if the export is not available. The `depth` field from `flattenTree`'s `FlatNode` provides indentation level.

    Footer: `<Button variant="outline" onClick={onCancel}>Cancel</Button>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "DeleteConfirmDialog|GroupPickerModal" | head -20</automated>
    <manual>Both files exist, compile without TS errors, match the interface signatures above.</manual>
  </verify>
  <done>Both files compile clean. Interfaces match spec. DeleteConfirmDialog mirrors ClearFormDialog pattern. GroupPickerModal renders a flat indented list from the TokenGroup tree with source group disabled.</done>
</task>

<task type="auto">
  <name>Task 2: BulkActionBar component</name>
  <files>
    src/components/tokens/BulkActionBar.tsx
    src/components/tokens/index.ts
  </files>
  <action>
    **BulkActionBar.tsx**

    Props interface (from RESEARCH.md Pattern 7):
    ```typescript
    interface BulkActionBarProps {
      selectedCount: number;
      selectedTokenPaths: string[];   // paths of selected tokens for live preview
      groups: TokenGroup[];           // masterGroups for GroupPickerModal
      sourceGroupId: string;          // passed through to GroupPickerModal
      detectedPrefix: string;         // pre-fill for remove-prefix input
      isReadOnly: boolean;            // hide bar entirely when true
      onDelete: () => void;
      onMoveToGroup: (destGroupId: string) => void;
      onChangeType: (type: TokenType) => void;
      onAddPrefix: (prefix: string) => void;
      onRemovePrefix: (prefix: string) => void;
      onClearSelection: () => void;
    }
    ```

    Structure:
    - If `isReadOnly` or `selectedCount === 0` → render `null`
    - Outer div: `className="flex items-center gap-2 px-3 py-2 bg-white border border-gray-200 rounded-lg shadow-sm mb-2"` (floats above table via normal flow — positioned by parent)
    - Selection count: `<span className="text-sm font-medium text-gray-700 mr-2">{selectedCount} selected</span>`
    - Clear button: `<button onClick={onClearSelection} className="ml-auto p-1 rounded hover:bg-gray-100" aria-label="Clear selection"><X className="w-4 h-4 text-gray-500" /></button>` (lucide-react `X` icon). Also capture `keydown` on the bar's outer div: `onKeyDown={(e) => { if (e.key === 'Escape') onClearSelection(); }}`
    - **Delete button**: `<Button variant="outline" size="sm" onClick={() => setDeleteOpen(true)}><Trash2 className="w-3.5 h-3.5 mr-1" />Delete</Button>`. Renders `<DeleteConfirmDialog open={deleteOpen} count={selectedCount} onConfirm={() => { onDelete(); setDeleteOpen(false); }} onCancel={() => setDeleteOpen(false)} />` always (controlled by `deleteOpen` local state)
    - **Move to group button**: `<Button variant="outline" size="sm" onClick={() => setMoveOpen(true)}><ArrowRight className="w-3.5 h-3.5 mr-1" />Move</Button>`. Renders `<GroupPickerModal open={moveOpen} groups={groups} sourceGroupId={sourceGroupId} onSelect={(id) => { onMoveToGroup(id); setMoveOpen(false); }} onCancel={() => setMoveOpen(false)} />`
    - **Change type select**: Render `<Select value="" onValueChange={(v) => onChangeType(v as TokenType)}>` with `<SelectTrigger className="h-8 w-36 text-xs">Change type</SelectTrigger>` and `<SelectContent>` listing all `TOKEN_TYPES` values as `<SelectItem>`. Import `TOKEN_TYPES` from `@/types/token.types` (verify this constant exists; if not, hardcode the known token types from the codebase: `['color','spacing','typography','border-radius','shadow','opacity','font-size','font-weight','line-height','letter-spacing','dimension','number','string','boolean','asset','other']`).
    - **Add Prefix**: Local state `addPrefixOpen: boolean`, `addPrefixValue: string`. When `!addPrefixOpen`: `<Button variant="outline" size="sm" onClick={() => setAddPrefixOpen(true)}>Add prefix</Button>`. When `addPrefixOpen`: expand inline within the bar — `<Input value={addPrefixValue} onChange={e => setAddPrefixValue(e.target.value)} placeholder="prefix-" className="h-7 w-32 text-xs" autoFocus />` + `<Button size="sm" onClick={() => { onAddPrefix(addPrefixValue); setAddPrefixOpen(false); setAddPrefixValue(''); }} disabled={!addPrefixValue}>Apply</Button>` + `<Button variant="ghost" size="sm" onClick={() => { setAddPrefixOpen(false); setAddPrefixValue(''); }}>×</Button>`. Below the input row, render a live preview list: take `selectedTokenPaths`, compute `${addPrefixValue}${path}` for each, show up to 5 as `<div className="text-xs text-gray-500 font-mono truncate">{newPath}</div>`; if more than 5, append `<div className="text-xs text-gray-400">+{selectedTokenPaths.length - 5} more</div>`.
    - **Remove Prefix**: Same inline expand pattern. Local state `removePrefixOpen: boolean`, `removePrefixValue: string` (initialize from `detectedPrefix` when opening). When `removePrefixOpen`: live preview shows `path.startsWith(removePrefixValue) ? path.slice(removePrefixValue.length) : path` (unchanged paths shown in muted color `text-gray-400`).

    **Update `src/components/tokens/index.ts`**: Add exports for `DeleteConfirmDialog`, `GroupPickerModal`, and `BulkActionBar`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "BulkActionBar|DeleteConfirmDialog|GroupPickerModal" | head -20</automated>
    <manual>BulkActionBar renders null when selectedCount=0 or isReadOnly. Delete dialog triggered by Delete button. Move modal triggered by Move button. Prefix inputs expand inline. All three files in index.ts barrel.</manual>
  </verify>
  <done>BulkActionBar, DeleteConfirmDialog, GroupPickerModal compile clean. BulkActionBar exports and is in index.ts. Inline prefix expand with live preview implemented. Change type select implemented with all token types. Escape key on bar calls onClearSelection.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit 2>&1 | grep -c error` returns 0. All three component files exist. `src/components/tokens/index.ts` exports all three.
</verification>

<success_criteria>
- `DeleteConfirmDialog.tsx`, `GroupPickerModal.tsx`, `BulkActionBar.tsx` all exist and compile
- `BulkActionBar` renders null when `selectedCount === 0` or `isReadOnly === true`
- `BulkActionBar` embeds both dialogs with controlled open state
- Inline prefix expand with live preview (up to 5 tokens, +N more)
- Change type select lists all token types
- Escape key calls `onClearSelection`
- TypeScript zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-row-actions/15-02-SUMMARY.md`
</output>
