---
phase: 01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals
plan: 08
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07]
files_modified: []
autonomous: false
requirements: [ATUI-UI-01, ATUI-UI-02, ATUI-UI-03, ATUI-UI-04, ATUI-UI-05, ATUI-UI-06]

must_haves:
  truths:
    - "All at-* elements render with ATUI styles applied (shadow root visible in DevTools)"
    - "Tab navigation works: View Tokens and Generate Tokens tabs switch correctly"
    - "Collection selector works: selecting a collection loads token data"
    - "Color token fields in the generator show a native color picker (not a text input)"
    - "At least one dialog opens and closes correctly (e.g. Delete Collection confirmation)"
    - "No console errors about unregistered custom elements or hydration mismatches"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm the entire ATUI migration works correctly end-to-end in the browser. This checkpoint covers the full user-visible surface area of Phase 1.

Purpose: ATUI is a web components library — correct rendering requires visual confirmation that shadow DOM is attached and styles are applied. TypeScript compilation alone cannot verify visual correctness.
Output: Human approval that ATUI components render and function correctly across the app.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and scan for remaining native elements</name>
  <files></files>
  <action>
Run a build verification and grep for any remaining native HTML elements that should have been replaced:

```bash
# Verify TypeScript and build pass
cd /Users/user/Dev/atui-tokens-manager
npx tsc --noEmit
npm run build

# Scan for remaining native <button> elements in migrated components (excluding AtuiDevTest which is kept)
grep -n "<button" src/app/page.tsx src/components/CollectionSelector.tsx src/components/SharedCollectionHeader.tsx src/components/CollectionActions.tsx src/components/SaveCollectionDialog.tsx src/components/LoadCollectionDialog.tsx src/components/BuildTokensModal.tsx src/components/FigmaConfig.tsx src/components/GitHubConfig.tsx src/components/ExportToFigmaDialog.tsx src/components/ImportFromFigmaDialog.tsx src/components/TokenGeneratorFormNew.tsx 2>/dev/null

# Scan for remaining native <select> elements (excluding TokenTable which is not in scope)
grep -n "<select" src/app/page.tsx src/components/CollectionSelector.tsx src/components/GitHubConfig.tsx src/components/ExportToFigmaDialog.tsx src/components/ImportFromFigmaDialog.tsx src/components/TokenGeneratorFormNew.tsx 2>/dev/null

# Scan for remaining native text <input> elements in value column of token rows (color picker OK)
grep -n "type=\"text\"\|type='text'" src/components/CollectionActions.tsx src/components/SaveCollectionDialog.tsx src/components/GitHubConfig.tsx src/components/FigmaConfig.tsx src/components/ImportFromFigmaDialog.tsx src/components/TokenGeneratorFormNew.tsx 2>/dev/null
```

Report any remaining native elements found. If any are found that should have been replaced, create a gap closure task rather than proceeding with the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npm run build 2>&1 | tail -10</automated>
    <manual>Build output shows success; no TypeScript errors</manual>
  </verify>
  <done>Build passes; no unexpected native button/select/input elements found in migrated components</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify ATUI migration end-to-end</name>
  <what-built>
Complete ATUI component migration across all app UI:
- AtuiProvider registered globally in layout.tsx (defines custom elements once)
- page.tsx: at-tabs for View/Generate navigation, at-button for header actions
- CollectionSelector: at-select for collection picker
- SharedCollectionHeader: at-button for Save As / New Collection
- CollectionActions: at-button triggers + at-dialog modals + at-input for Rename/Duplicate
- SaveCollectionDialog / LoadCollectionDialog / BuildTokensModal: at-dialog + at-button + at-input
- FigmaConfig / GitHubConfig: at-dialog + at-input + at-select + at-button
- ExportToFigmaDialog / ImportFromFigmaDialog: at-dialog + at-select + at-input + at-button
- TokenGeneratorFormNew: at-button for all actions, at-select for token type, at-input for all text fields, native color picker for color tokens
  </what-built>
  <how-to-verify>
Start dev server: `npm run dev` in the project directory. Open http://localhost:3000

**1. ATUI styles applied (critical):**
- Open DevTools → Inspect any visible `at-button` element
- Confirm it has a shadow root (shows `#shadow-root` in Elements panel)
- Confirm buttons have ATUI styling (not bare/unstyled)

**2. Tab navigation:**
- Confirm "View Tokens" and "Generate Tokens" tabs are visible as ATUI tabs
- Click "Generate Tokens" tab — page switches to generate view
- Click "View Tokens" tab — page switches back

**3. Collection selector:**
- The collection dropdown in the header bar should render as an ATUI select
- Select a different collection — token table updates

**4. Header buttons:**
- "Build Tokens" button should appear as an ATUI button in the app header
- Click it (if a collection is loaded) — BuildTokensModal opens as at-dialog

**5. Color picker in Generate tab:**
- Switch to Generate Tokens tab
- Click "+ Add Token" in any group
- In the new token row, the type defaults to "color"
- Confirm the value field shows a native color picker (not a text input)
- Click/interact with the color picker — value updates live as you drag

**6. Delete/Rename/Duplicate dialogs:**
- Select a MongoDB collection from the selector
- Delete/Rename/Duplicate buttons appear in the header
- Click "Rename" — at-dialog opens with at-input for name
- Type a new name — confirm input accepts text
- Click "Cancel" — dialog closes

**7. Configure Figma / GitHub:**
- Click "Configure Figma" button — at-dialog opens with ATUI inputs
- Click Cancel — dialog closes

**8. No console errors:**
- Open browser console
- Confirm NO errors about: "Custom element not registered", "Hydration mismatch", "defineCustomElements is not a function"
  </how-to-verify>
  <resume-signal>Type "approved" to mark phase complete, or describe any issues found</resume-signal>
  <action>Human visual verification — see how-to-verify steps above</action>
  <verify>
    <automated>NONE — visual verification required</automated>
    <manual>Follow how-to-verify steps above in the browser at http://localhost:3000</manual>
  </verify>
  <done>User types "approved" confirming ATUI components render with styles and all interactive flows work</done>
</task>

</tasks>

<verification>
Human confirmed all ATUI components render with styles, tabs work, color picker appears for color tokens, dialogs open/close correctly, no console errors.
</verification>

<success_criteria>
User approves the ATUI migration. All ATUI components render with shadow DOM and correct styles. All interactive flows work: tabs, collection management, dialogs, color picker, config panels.
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals/01-08-SUMMARY.md`

Also update `.planning/STATE.md` and `.planning/ROADMAP.md` to mark Phase 1 (v1.1) as complete.
</output>
