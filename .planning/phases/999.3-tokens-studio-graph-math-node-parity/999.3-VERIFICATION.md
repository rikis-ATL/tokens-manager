# Phase 999.3 — Verification (human UAT)

**Automated baseline:** `yarn test mathExpression graphMathOps`

## Browser checklist (collections → group → graph → Math node)

- [ ] **Ops mode — sample operations**
  - [ ] Absolute: negative input → positive output
  - [ ] Add variadic: wired `inputs` array **or** CSV fallback sums correctly
  - [ ] Divide variadic: sequential divide matches expectation (e.g. 100÷5÷20 = 1)
  - [ ] Closest number: list + target → value / index / difference outputs wire correctly
  - [ ] Fluid: change min/max size and viewport fields; wire **A** as viewport override
  - [ ] Count: array on **A** → length
  - [ ] Difference: |a−b| with precision
  - [ ] Cosine / Exponentiation: sanity spot-check vs calculator
- [ ] **Expr mode — Evaluate Math**
  - [ ] Formula uses `a` and `b` with wired or fallback `bExpr`
  - [ ] Invalid formula shows error; blur persists expression
- [ ] **Regression**
  - [ ] Existing graphs with legacy Math configs still load and evaluate
  - [ ] No duplicate/conflicting handles when switching operation vs expression mode

## Sign-off

- [ ] Tester initials / date: _______________

When complete, update `999.3-PLAN.md` frontmatter `status` to `complete` and note completion date in ROADMAP backlog entry.
