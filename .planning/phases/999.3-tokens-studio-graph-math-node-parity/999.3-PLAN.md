---
phase: 999.3
plan: 01
type: execute
wave: 1
depends_on: []
status: implemented_pending_verification
requirements:
  - MATH-TS-01
  - MATH-TS-02
  - MATH-TS-03

must_haves:
  truths:
    - "Math node exposes operations named and behaving consistently with Tokens Studio math docs for the supported set"
    - "Expression mode supports variables a and b for multi-input formulas"
    - "Automated tests cover representative operations including variadic, closest, fluid, and expression b"
---

<objective>
Deliver **Tokens Studio–aligned** Math composable operations and **Evaluate Math** (expression) enhancements in the token graph: types, evaluator, UI, defaults, and tests. Close backlog when human UAT in `999.3-VERIFICATION.md` is complete.

Output (code):
- `src/types/graph-nodes.types.ts` — `MathOp` / `MathConfig` extensions
- `src/lib/graphEvaluator.ts` — `evalMath` branches + helpers
- `src/lib/mathExpression.ts` — variable `b`
- `src/components/graph/nodes/MathNode.tsx` — operations UI, ports (`inputs`, `numbers`, closest outputs)
- `src/components/graph/GroupStructureGraph.tsx` — default math config fields
- `src/lib/__tests__/graphMathOps.test.ts`, `mathExpression.test.ts`

NON-GOALS: npm publish, org tenancy, MCP changes.
</objective>

<execution_context>
@.planning/phases/999.3-tokens-studio-graph-math-node-parity/999.3-CONTEXT.md
</execution_context>

## Task breakdown

1. **Types** — Extend `MathOp` and `MathConfig` (fluid fields, variadic/closest CSV fallbacks, `bExpr`).
2. **Evaluator** — Implement `addVariadic`, `divideVariadic`, `closestNumber` (+ index/diff outputs), `fluid`, `count`, `difference`, unary ops (`absolute`, `ceiling`, `cosine`, `exponentiation`); wire expression `b` into `computeMathExpressionResult`.
3. **UI** — Operation picker, conditional rows/handles; closest: three source handles; expression: Bind `b`.
4. **Tests** — Jest for key operations and `a + b` expression.
5. **Verification** — Complete `999.3-VERIFICATION.md` (human browser pass).

## Verification

See `999.3-VERIFICATION.md`. Automated: `yarn test mathExpression graphMathOps`.

## Promotion

When verified, optionally promote to an active milestone with `/gsd-review-backlog` and add formal REQ IDs to `.planning/REQUIREMENTS.md` traceability table.
