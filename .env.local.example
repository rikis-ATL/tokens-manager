# MongoDB connection string (fallback — can also be configured via Settings > Database UI)
# Format: mongodb://[username:password@]host[:port]/database
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/dbname
MONGODB_URI=mongodb://localhost:27017/atui-tokens-manager

# MongoDB Collection Name (optional)
# Default: 'tokencollections'
# Override to use a different collection (e.g., 'tokenCollections' for demo data)
# MONGODB_COLLECTION_NAME=tokencollections

# Public demo site (optional)
# When true: `/` shows the marketing landing. Org Users → Invite as Demo with email = DEMO_ADMIN_EMAIL creates the shared demo user immediately (no invite email; password from DEMO_ADMIN_PASSWORD).
DEMO_MODE=false
# Shared demo org id (Organization._id). When set, demo invite only works when signed-in admin’s org matches.
# DEMO_ORG_ID=
# DEMO_ADMIN_EMAIL=
# DEMO_ADMIN_PASSWORD=
# Optional: self-hosted CTA on landing (default mailto in UI if unset)
# NEXT_PUBLIC_DEMO_CONTACT_HREF=mailto:you@example.com

# Playground collection ID — the MongoDB _id of the collection that serves as the hero playground.
# When DEMO_MODE=true and this is set, middleware redirects unauthenticated visitors hitting
# /collections/${PLAYGROUND_COLLECTION_ID}/tokens to /auth/auto-demo for automatic sign-in.
# PLAYGROUND_COLLECTION_ID=

# App shell theming (optional) — MongoDB id of the token collection that drives shadcn CSS variables for the UI chrome
# APP_THEME_COLLECTION_ID=

# Self-hosted deployment bypass (Phase 23 D-14)
# When true, all billing limit checks (collection count, token count, export rate, rate limiter)
# short-circuit before any DB read. Set to true for internal/self-hosted installs.
# Defaults to false/unset = limits active.
SELF_HOSTED=false
